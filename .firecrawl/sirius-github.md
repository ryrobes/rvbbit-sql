[Skip to content](https://github.com/sirius-db/sirius#start-of-content)

You signed in with another tab or window. [Reload](https://github.com/sirius-db/sirius) to refresh your session.You signed out in another tab or window. [Reload](https://github.com/sirius-db/sirius) to refresh your session.You switched accounts on another tab or window. [Reload](https://github.com/sirius-db/sirius) to refresh your session.Dismiss alert

{{ message }}

### Uh oh!

There was an error while loading. [Please reload this page](https://github.com/sirius-db/sirius).

[sirius-db](https://github.com/sirius-db)/ **[sirius](https://github.com/sirius-db/sirius)** Public

- [Notifications](https://github.com/login?return_to=%2Fsirius-db%2Fsirius) You must be signed in to change notification settings
- [Fork\\
104](https://github.com/login?return_to=%2Fsirius-db%2Fsirius)
- [Star\\
1k](https://github.com/login?return_to=%2Fsirius-db%2Fsirius)


dev

[**9** Branches](https://github.com/sirius-db/sirius/branches) [**0** Tags](https://github.com/sirius-db/sirius/tags)

[Go to Branches page](https://github.com/sirius-db/sirius/branches)[Go to Tags page](https://github.com/sirius-db/sirius/tags)

Go to file

Code

Open more actions menu

## Folders and files

| Name | Name | Last commit message | Last commit date |
| --- | --- | --- | --- |
| ## Latest commit<br>![bwyogatama](https://avatars.githubusercontent.com/u/34973829?v=4&size=40)![claude](https://avatars.githubusercontent.com/u/81847?v=4&size=40)<br>[bwyogatama](https://github.com/sirius-db/sirius/commits?author=bwyogatama)<br>and<br>[claude](https://github.com/sirius-db/sirius/commits?author=claude)<br>[feat(tpch\_performance): benchmark DuckDB-native datasets via --data-s…](https://github.com/sirius-db/sirius/commit/735991aac4dbb09394eb0ed5cc665d0285d98256)<br>Open commit detailssuccess<br>2 hours agoJul 2, 2026<br>[735991a](https://github.com/sirius-db/sirius/commit/735991aac4dbb09394eb0ed5cc665d0285d98256) · 2 hours agoJul 2, 2026<br>## History<br>[1,034 Commits](https://github.com/sirius-db/sirius/commits/dev/) <br>Open commit details<br>[View commit history for this file.](https://github.com/sirius-db/sirius/commits/dev/) 1,034 Commits |
| [.claude](https://github.com/sirius-db/sirius/tree/dev/.claude ".claude") | [.claude](https://github.com/sirius-db/sirius/tree/dev/.claude ".claude") | [feat(tpch\_performance): benchmark DuckDB-native datasets via --data-s…](https://github.com/sirius-db/sirius/commit/735991aac4dbb09394eb0ed5cc665d0285d98256 "feat(tpch_performance): benchmark DuckDB-native datasets via --data-source (#1044)  ## What  Adds a `--data-source {parquet,duckdb}` option to the TPC-H performance harness (`test/tpch_performance/performance_test.py`) so it can benchmark **DuckDB-native datasets** — both scanned from disk and pinned into the Sirius cache — in addition to parquet, across all four modes (`grouped`, `sequential`, `isolated`, `nsys-profile`). Also makes the per-query log split robust for the duckdb source and updates the related skills/docs.  ## Changes  - **`performance_test.py`** — new `--data-source` flag; source-aware `--input` validation (a parquet directory vs a single `.duckdb` file); `open_connection` opens the `.duckdb` file read-only and queries its native tables (no `read_parquet` views) for the duckdb source; `data_source` threaded through the three timing runners, the nsys-profile subprocess (opens the db file as the CLI database, skips view   scaffolding), `metadata.json`, and startup logs. - **`tpch_pin_columns.py`** — emits `CALL pin_table(format='duckdb', name=…, cols=[…])` (no glob path) for the duckdb source via a shared `_pin_call` helper; parquet emission   unchanged; optional `--format` on the CLI. - **`split_sirius_log`** — identifies per-query log boundaries by matching the logged SQL against the known queries instead of counting/filtering statements. This fixes the duckdb source, which has no `CREATE VIEW` lines and previously double-counted the per-query `SET gpu_execution` toggles (so the split was skipped). Now robust across   parquet/duckdb, pin/no-pin, and all iteration modes. - **docs/skills** — document `--data-source` and duckdb pinning in `test/tpch_performance/CLAUDE.md` and the `/benchmark` skill; `/benchmark` now always   confirms every parameter before running; data generation delegates to   `/dataset-manager`, which surfaces the duckdb-only `--cluster` option.  No engine/C++ changes — `pin_table(format='duckdb')` and the GPU-native scan already exist.  ## Testing  Built the worktree and ran on GPU (NVIDIA GB10) at SF1 and SF10:  - **Correctness**: `--data-source duckdb --engine both --validation` → 3/3 queries match the DuckDB CPU baseline (byte-exact / within `1e-10`) at both scale factors. CPU-only   duckdb results are byte-identical to the parquet path. - **Pinning**: `--pin gpu` over the duckdb source runs the pin→run→unpin cycle cleanly   (the extension accepts the `format='duckdb'` pin SQL). - **`split_sirius_log`**: per-query logs split correctly across the matrix —   {parquet, duckdb} × {pin, no-pin} × {grouped, sequential, isolated}   (nsys-profile uses per-subprocess logs and isn't split). - `pre-commit run` passes on all changed files.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | 2 hours agoJul 2, 2026 |
| [.github](https://github.com/sirius-db/sirius/tree/dev/.github ".github") | [.github](https://github.com/sirius-db/sirius/tree/dev/.github ".github") | [build(pixi): bump to 0.71 and adopt rich platforms (](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") [#1005](https://github.com/sirius-db/sirius/pull/1005) [)](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | 3 days agoJun 30, 2026 |
| [cmake](https://github.com/sirius-db/sirius/tree/dev/cmake "cmake") | [cmake](https://github.com/sirius-db/sirius/tree/dev/cmake "cmake") | [S3 change to align with scan-manager cleanup (](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") [#913](https://github.com/sirius-db/sirius/pull/913) [) (](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") [#923](https://github.com/sirius-db/sirius/pull/923) [)](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") | 3 weeks agoJun 12, 2026 |
| [cucascade @ 6324c15](https://github.com/NVIDIA/cuCascade/tree/6324c15286f61120d195ded2cd34a365de39882a "cucascade") | [cucascade @ 6324c15](https://github.com/NVIDIA/cuCascade/tree/6324c15286f61120d195ded2cd34a365de39882a "cucascade") | [Use](https://github.com/sirius-db/sirius/commit/b2557167c5c5b200f099fa033eeb4819227769bf "Use `data_batch` standardized on `std::shared_ptr` (#1043)  Update Sirius for cuCascade’s shared-only data_batch API:  - Construct batches through data_batch::make() and use the non-templated repository manager. - Respect const read-only representations in result collection. - Replace destructive table-scan projection with the existing zero-copy, view-backed ownership   pattern. - Add regression coverage verifying projection does not invalidate its input.")`data_batch` [standardized on](https://github.com/sirius-db/sirius/commit/b2557167c5c5b200f099fa033eeb4819227769bf "Use `data_batch` standardized on `std::shared_ptr` (#1043)  Update Sirius for cuCascade’s shared-only data_batch API:  - Construct batches through data_batch::make() and use the non-templated repository manager. - Respect const read-only representations in result collection. - Replace destructive table-scan projection with the existing zero-copy, view-backed ownership   pattern. - Add regression coverage verifying projection does not invalidate its input.")`std::shared_ptr` [(](https://github.com/sirius-db/sirius/commit/b2557167c5c5b200f099fa033eeb4819227769bf "Use `data_batch` standardized on `std::shared_ptr` (#1043)  Update Sirius for cuCascade’s shared-only data_batch API:  - Construct batches through data_batch::make() and use the non-templated repository manager. - Respect const read-only representations in result collection. - Replace destructive table-scan projection with the existing zero-copy, view-backed ownership   pattern. - Add regression coverage verifying projection does not invalidate its input.") [#1043](https://github.com/sirius-db/sirius/pull/1043) [)](https://github.com/sirius-db/sirius/commit/b2557167c5c5b200f099fa033eeb4819227769bf "Use `data_batch` standardized on `std::shared_ptr` (#1043)  Update Sirius for cuCascade’s shared-only data_batch API:  - Construct batches through data_batch::make() and use the non-templated repository manager. - Respect const read-only representations in result collection. - Replace destructive table-scan projection with the existing zero-copy, view-backed ownership   pattern. - Add regression coverage verifying projection does not invalidate its input.") | 15 hours agoJul 2, 2026 |
| [docs](https://github.com/sirius-db/sirius/tree/dev/docs "docs") | [docs](https://github.com/sirius-db/sirius/tree/dev/docs "docs") | [docs: refresh Super Sirius docs for the IO framework and unified scan…](https://github.com/sirius-db/sirius/commit/148c2a5eb0d17b313fee2f2b937e57e0944b528e "docs: refresh Super Sirius docs for the IO framework and unified scan operator (#1039)  Brings the Super Sirius reference docs (`docs/super-sirius/`) up to the current design, from the stale last-updated marker (`46e7cb06`, #873 — 97 commits back) to HEAD `84543810a` (#997, \"updated IO framework\"). This is a gap-fill against the current docs, not a blanket re-document.  ## Highlights  - **scan.md** (major rewrite): unified `sirius_gpu_scan_operator` (`GPU_SCAN`) + per-format `gpu_ingestible` (parquet / DuckDB-native), scan manager (`split_provider` + `load_balancing_scan_batch_coalescer` + round-robin device placement), pinned tables (2 formats × 2 tiers), `owning_table_view`, DuckDB-native decode + two-phase metadata walk, dual-path row-group pruning, and the new `io/` reactor layer (io_uring / REST-S3 / kvikio + `io/cache` prefetching cache). Removed sections describing deleted components (scan tasks, scan executor, `cache_level`, prefetched data source). - **configuration.md**: new `executor.scan_manager` schema (IO reactors, prefetch cache, `object_store` credentials + nested `local`/`rest`/`cache`), refreshed example config, updated `operator_params` and SET variables; dropped the removed `duckdb_scan` executor / `scan_cache_level`. - **optimizations.md**: added entries for adaptive MARK build side, projection folding, DuckDB-native metadata walk + async coalesced reads, async S3/REST backend, byte-based sort-sample merge, pinned/cached zero-copy, partial-read prefetching cache, and load-balanced scan coalescing; corrected relocated code paths; dropped two fully-superseded entries. - **operators / expression-executor / physical-plan-generation / architecture-overview / execution-flow / memory-management / pipeline-execution / multi-gpu-architecture / task-creator / dynamic-filters**: aligned with the current design. - Bumped the docs `last-updated-commit` marker to `84543810a`.  ## Note: Iceberg GPU scan removed from docs  Iceberg GPU-scan coverage was **removed** because the read/task path is not currently wired into the build: `src/op/scan/iceberg_scan_task.cpp` is excluded from `CMakeLists.txt` and still includes the deleted `parquet_scan_task.hpp`; there is no iceberg `gpu_ingestible` and no `task_creator` branch. The iceberg operator and delete-filter machinery are still compiled, but the GPU read path is mid-migration onto the unified `gpu_ingestible` framework. Worth tracking separately.  ## Verification  - Docs-only change (no source edits). - `pre-commit` (trailing-whitespace, end-of-file, mixed-line-ending, smartquotes, codespell) passes. - No remaining references to removed symbols (`parquet_split_provider`, `prefetched_data_source`, `duckdb_scan_executor`, `s3_ioctx`, `cache_level`, `GPU_PARQUET_SCAN`, `GPU_DUCKDB_NATIVE_SCAN`, …) or Iceberg across the docs; scan.md intra-doc anchor links resolve.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | yesterdayJul 1, 2026 |
| [duckdb @ 08e34c4](https://github.com/duckdb/duckdb/tree/08e34c447bae34eaee3723cac61f2878b6bdf787 "duckdb") | [duckdb @ 08e34c4](https://github.com/duckdb/duckdb/tree/08e34c447bae34eaee3723cac61f2878b6bdf787 "duckdb") | [chore(deps): bump DuckDB to 1.5.4 (](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") [#970](https://github.com/sirius-db/sirius/pull/970) [)](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") | last weekJun 26, 2026 |
| [duckdb-python @ 305369d](https://github.com/duckdb/duckdb-python/tree/305369d48d2b7e3460c36f00842200b560d6447d "duckdb-python") | [duckdb-python @ 305369d](https://github.com/duckdb/duckdb-python/tree/305369d48d2b7e3460c36f00842200b560d6447d "duckdb-python") | [chore(deps): bump DuckDB to 1.5.4 (](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") [#970](https://github.com/sirius-db/sirius/pull/970) [)](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") | last weekJun 26, 2026 |
| [experimental/starrocks](https://github.com/sirius-db/sirius/tree/dev/experimental/starrocks "This path skips through empty directories") | [experimental/starrocks](https://github.com/sirius-db/sirius/tree/dev/experimental/starrocks "This path skips through empty directories") | [chore(starrocks): bump StarRocks to 4.1.1 (](https://github.com/sirius-db/sirius/commit/c91379ed3ad66a69bd099fb4d30e8893ea79cbdd "chore(starrocks): bump StarRocks to 4.1.1 (#1053)  https://github.com/StarRocks/starrocks/releases/tag/4.1.1") [#1053](https://github.com/sirius-db/sirius/pull/1053) [)](https://github.com/sirius-db/sirius/commit/c91379ed3ad66a69bd099fb4d30e8893ea79cbdd "chore(starrocks): bump StarRocks to 4.1.1 (#1053)  https://github.com/StarRocks/starrocks/releases/tag/4.1.1") | 4 hours agoJul 2, 2026 |
| [rust](https://github.com/sirius-db/sirius/tree/dev/rust "rust") | [rust](https://github.com/sirius-db/sirius/tree/dev/rust "rust") | [feat(telemetry): expose a quent-open Viewer entry for Sirius artifacts (](https://github.com/sirius-db/sirius/commit/1deec86ce6984a5197e0868268dd456f74b36476 "feat(telemetry): expose a quent-open Viewer entry for Sirius artifacts (#1020)  Prepares Sirius telemetry artifacts to open in quent's config-free `quent-open` viewer: declares the analyzer package in the `model!` macro (`analyzer:`) and adds a `Viewer`/`QuentViewer` entry that quent-open clones + builds straight from the `model.qmi` sidecar.  ✅ **The full quent-open stack (rapidsai/quent#260–#266) has landed on quent main**, so this pins `rapidsai/quent` main directly — no fork, no remaining blocker. (Bumps quent past dev's current pin.)  Verified end-to-end on GPU: build → telemetry → `quent-open local` clones the sources, builds the viewer, serves (`GET /` → 200, `/api/engines` lists the engine).  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") | yesterdayJul 1, 2026 |
| [scripts](https://github.com/sirius-db/sirius/tree/dev/scripts "scripts") | [scripts](https://github.com/sirius-db/sirius/tree/dev/scripts "scripts") | [Removed references to deprecated config options post new io framework (](https://github.com/sirius-db/sirius/commit/87cdd2aaaaeb2ef0bb16ccfe4814b7c7750a94e1 "Removed references to deprecated config options post new io framework (#1037)") […](https://github.com/sirius-db/sirius/pull/1037) | 2 days agoJun 30, 2026 |
| [src](https://github.com/sirius-db/sirius/tree/dev/src "src") | [src](https://github.com/sirius-db/sirius/tree/dev/src "src") | [Added ARRAY data type (fixed-sized lists) (](https://github.com/sirius-db/sirius/commit/a1ab4288feb7681ec704c0ada1089270f80cc472 "Added ARRAY data type (fixed-sized lists) (#901)  ## Add ARRAY (fixed-size list) data type support for native scan path  ### Summary - Maps DuckDB's ARRAY type to cuDF LIST in the type system. - cuCascade handles the H2D transfer and memory management for the LIST column. - list_offset_fixup.cpp/.hpp: a temporary workaround (see [#147](https://github.com/NVIDIA/cuCascade/issues/147)).  ### Changes **Type system** - `src/include/helper/logical_type.hpp`: add the ARRAY logical type. - `src/helper/type_conversions.cpp`: ARRAY to/from DuckDB conversion. - `src/include/cudf/cudf_utils.hpp`: map ARRAY to/from cuDF LIST.  **Scan - Metadata walk** - `src/include/op/scan/duckdb_native_metadata.hpp`: add ARRAY child-segment buckets and is_array flag to column metadata. - `src/op/scan/duckdb_native_metadata.cpp`: walk an ARRAY column's three segment trees into their buckets.  **Scan - Decode** - `src/op/scan/duckdb_native_decoder.{cpp,hpp}`: stage/decode ARRAY columns on the GPU; enable ALP/ALPRD codecs.  **Read-back** - `src/include/op/result/host_table_chunk_reader.hpp`: add child mask accessor / child null count fields. - `src/op/result/host_table_chunk_reader.cpp`: copy element-level child validity back into the DuckDB ARRAY vector.  **LIST offset fixup** - `src/helper/list_offset_fixup.cpp`, `src/include/cudf/list_offset_fixup.hpp`: recast LIST offsets back to INT32 after cuCascade's H2D promotes them to INT64. - `src/include/pipeline/batch_lock_utils.hpp`: apply the fixup after the H2D conversion. - `CMakeLists.txt`: add list_offset_fixup.cpp and tests to the build.  **Tests** - `test/cpp/helper/test_logical_type.cpp`: ARRAY logical type tests. - `test/cpp/helper/test_cudf_utils.cpp`: ARRAY to cuDF LIST mapping tests. - `test/cpp/scan/test_duckdb_native_walker.cpp`: unit tests for the ARRAY metadata walk. - `test/cpp/integration/test_gpu_execution_array.cpp`: end-to-end ARRAY scan tests. - `test/cpp/utils/gpu_execution_fixture.hpp`: shared file-backed native-scan fixture + GPU-vs-CPU comparator") [#901](https://github.com/sirius-db/sirius/pull/901) [)](https://github.com/sirius-db/sirius/commit/a1ab4288feb7681ec704c0ada1089270f80cc472 "Added ARRAY data type (fixed-sized lists) (#901)  ## Add ARRAY (fixed-size list) data type support for native scan path  ### Summary - Maps DuckDB's ARRAY type to cuDF LIST in the type system. - cuCascade handles the H2D transfer and memory management for the LIST column. - list_offset_fixup.cpp/.hpp: a temporary workaround (see [#147](https://github.com/NVIDIA/cuCascade/issues/147)).  ### Changes **Type system** - `src/include/helper/logical_type.hpp`: add the ARRAY logical type. - `src/helper/type_conversions.cpp`: ARRAY to/from DuckDB conversion. - `src/include/cudf/cudf_utils.hpp`: map ARRAY to/from cuDF LIST.  **Scan - Metadata walk** - `src/include/op/scan/duckdb_native_metadata.hpp`: add ARRAY child-segment buckets and is_array flag to column metadata. - `src/op/scan/duckdb_native_metadata.cpp`: walk an ARRAY column's three segment trees into their buckets.  **Scan - Decode** - `src/op/scan/duckdb_native_decoder.{cpp,hpp}`: stage/decode ARRAY columns on the GPU; enable ALP/ALPRD codecs.  **Read-back** - `src/include/op/result/host_table_chunk_reader.hpp`: add child mask accessor / child null count fields. - `src/op/result/host_table_chunk_reader.cpp`: copy element-level child validity back into the DuckDB ARRAY vector.  **LIST offset fixup** - `src/helper/list_offset_fixup.cpp`, `src/include/cudf/list_offset_fixup.hpp`: recast LIST offsets back to INT32 after cuCascade's H2D promotes them to INT64. - `src/include/pipeline/batch_lock_utils.hpp`: apply the fixup after the H2D conversion. - `CMakeLists.txt`: add list_offset_fixup.cpp and tests to the build.  **Tests** - `test/cpp/helper/test_logical_type.cpp`: ARRAY logical type tests. - `test/cpp/helper/test_cudf_utils.cpp`: ARRAY to cuDF LIST mapping tests. - `test/cpp/scan/test_duckdb_native_walker.cpp`: unit tests for the ARRAY metadata walk. - `test/cpp/integration/test_gpu_execution_array.cpp`: end-to-end ARRAY scan tests. - `test/cpp/utils/gpu_execution_fixture.hpp`: shared file-backed native-scan fixture + GPU-vs-CPU comparator") | 3 hours agoJul 2, 2026 |
| [substrait @ 6b70ad4](https://github.com/sirius-db/duckdb-substrait-extension/tree/6b70ad434c78781a26dc58da045c3279358b9411 "substrait") | [substrait @ 6b70ad4](https://github.com/sirius-db/duckdb-substrait-extension/tree/6b70ad434c78781a26dc58da045c3279358b9411 "substrait") | [chore(deps): bump DuckDB to 1.5.4 (](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") [#970](https://github.com/sirius-db/sirius/pull/970) [)](https://github.com/sirius-db/sirius/commit/d1960b130f9014299e74e7f7db14b510481839d4 "chore(deps): bump DuckDB to 1.5.4 (#970)  https://duckdb.org/2026/06/17/announcing-duckdb-154 https://github.com/duckdb/duckdb/releases/tag/v1.5.4  Draft because this is waiting on the conda package for 1.5.4 to update our pixi env.") | last weekJun 26, 2026 |
| [test](https://github.com/sirius-db/sirius/tree/dev/test "test") | [test](https://github.com/sirius-db/sirius/tree/dev/test "test") | [feat(tpch\_performance): benchmark DuckDB-native datasets via --data-s…](https://github.com/sirius-db/sirius/commit/735991aac4dbb09394eb0ed5cc665d0285d98256 "feat(tpch_performance): benchmark DuckDB-native datasets via --data-source (#1044)  ## What  Adds a `--data-source {parquet,duckdb}` option to the TPC-H performance harness (`test/tpch_performance/performance_test.py`) so it can benchmark **DuckDB-native datasets** — both scanned from disk and pinned into the Sirius cache — in addition to parquet, across all four modes (`grouped`, `sequential`, `isolated`, `nsys-profile`). Also makes the per-query log split robust for the duckdb source and updates the related skills/docs.  ## Changes  - **`performance_test.py`** — new `--data-source` flag; source-aware `--input` validation (a parquet directory vs a single `.duckdb` file); `open_connection` opens the `.duckdb` file read-only and queries its native tables (no `read_parquet` views) for the duckdb source; `data_source` threaded through the three timing runners, the nsys-profile subprocess (opens the db file as the CLI database, skips view   scaffolding), `metadata.json`, and startup logs. - **`tpch_pin_columns.py`** — emits `CALL pin_table(format='duckdb', name=…, cols=[…])` (no glob path) for the duckdb source via a shared `_pin_call` helper; parquet emission   unchanged; optional `--format` on the CLI. - **`split_sirius_log`** — identifies per-query log boundaries by matching the logged SQL against the known queries instead of counting/filtering statements. This fixes the duckdb source, which has no `CREATE VIEW` lines and previously double-counted the per-query `SET gpu_execution` toggles (so the split was skipped). Now robust across   parquet/duckdb, pin/no-pin, and all iteration modes. - **docs/skills** — document `--data-source` and duckdb pinning in `test/tpch_performance/CLAUDE.md` and the `/benchmark` skill; `/benchmark` now always   confirms every parameter before running; data generation delegates to   `/dataset-manager`, which surfaces the duckdb-only `--cluster` option.  No engine/C++ changes — `pin_table(format='duckdb')` and the GPU-native scan already exist.  ## Testing  Built the worktree and ran on GPU (NVIDIA GB10) at SF1 and SF10:  - **Correctness**: `--data-source duckdb --engine both --validation` → 3/3 queries match the DuckDB CPU baseline (byte-exact / within `1e-10`) at both scale factors. CPU-only   duckdb results are byte-identical to the parquet path. - **Pinning**: `--pin gpu` over the duckdb source runs the pin→run→unpin cycle cleanly   (the extension accepts the `format='duckdb'` pin SQL). - **`split_sirius_log`**: per-query logs split correctly across the matrix —   {parquet, duckdb} × {pin, no-pin} × {grouped, sequential, isolated}   (nsys-profile uses per-subprocess logs and isn't split). - `pre-commit run` passes on all changed files.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | 2 hours agoJul 2, 2026 |
| [test\_datasets](https://github.com/sirius-db/sirius/tree/dev/test_datasets "test_datasets") | [test\_datasets](https://github.com/sirius-db/sirius/tree/dev/test_datasets "test_datasets") | [Iceberg V3 + equality deletes format coherent + schema evolution + sn…](https://github.com/sirius-db/sirius/commit/e8e2f42340211e4596eb28a30aa5fa5bfdbea460 "Iceberg V3 + equality deletes format coherent + schema evolution + snapshot-aware deletes (#574)  ## Summary  Makes Iceberg tables work correctly on GPU for real-world tables — not just our simple test fixtures. Before this PR, GPU iceberg scans silently returned wrong results on tables written by Spark, REST catalogs, or any tool that uses standard Iceberg features like equality deletes, schema evolution, or partition changes.  **Depends on:** #570 (hive partition columns)  ---  ## Problems Fixed  **1. Heterogeneous equality delete schemas**  Real Iceberg tables can have multiple equality delete files with different key columns (e.g., one delete file keyed on `name`, another on `name+birthdate`). Our GPU path used a single hash join, which broke when delete files had different schemas.  Fix: Changed from a single hash join to `vector<EqualityDeleteGroup>`, each with its own GPU hash join.  **2. Schema evolution produced wrong data**  When an Iceberg table adds a column (e.g., `ALTER TABLE ADD COLUMN color`), old parquet files don't have that column. DuckDB handles this by injecting NULL values. Our GPU path either returned empty strings or crashed.  Fix: Detect which columns each file has, give each file its own cuDF reader projection, and inject typed NULL columns for missing ones after reading.  **3. Snapshot time-travel applied wrong deletes**  Querying a historical snapshot (`snapshot_from_id = X`) should only see deletes from that snapshot. We always read deletes from the latest snapshot, so historical queries could show fewer rows than they should.  Fix: Pass the snapshot ID to `iceberg_metadata()` so DuckDB gives us the correct manifest for that snapshot.  **4. Equality deletes applied to wrong data files**  Iceberg tracks when each file was written (sequence number). A delete at sequence 5 should only affect data files written before sequence 5 — not files written after. We applied all deletes to all files.  Fix: Group equality deletes by sequence number. At scan time, skip any delete group whose sequence number isn't higher than the data file's. This is a CPU-side check — no extra GPU work.  **5. Queries that don't SELECT the delete key columns skipped deletes**  `SELECT count(tx_id) FROM iceberg_table` would return all rows because `user_id` (the equality delete key) wasn't in the projection. DuckDB's optimizer prunes columns not in the query, so the delete filter couldn't find the key columns to match against.  Fix: Force equality delete key columns into the scan projection, then strip them after delete filtering so downstream operators see only the requested columns.  **6. REST catalog tables had no delete support**  Tables accessed via REST catalog (`ATTACH '' AS rc (TYPE iceberg, ...); SELECT FROM rc.schema.table`) had no deletes applied because the table path wasn't available through the normal parameter mechanism.  Fix: Derive the table path from the first data file in the bind data. Also switched the main discovery path to use DuckDB's `iceberg_metadata()` function instead of our custom Avro reader — this automatically handles all manifest versions, compression codecs, and catalog types.  **7. Partition evolution crashed**  Some Iceberg tables change their partition scheme over time (e.g., `event_type` starts as a data column in the parquet file, then later becomes a partition value in the directory path). Our code assumed all files have the same column layout and crashed with an out-of-bounds access.  Fix: Unified inject function that checks per-file whether each column comes from parquet data, from the file path, or is truly missing (NULL). Replaces the previous two-stage approach that couldn't handle per-file column count variation.  ---  ## Test Plan  **Unit Tests**  - 27/27 pass: 21 iceberg tests + 6 hive partition tests (including filter and `GROUP BY` on partition columns)  **Stress Tests**  *DuckDB Iceberg Extension Fixtures* — 34 real-world tables from the `duckdb/duckdb_iceberg` test suite, generated by Spark, PyIceberg, and DuckDB. Covers V1/V2 tables, equality deletes, positional deletes, hive partitioning, partition evolution, and various data types. Compared GPU output against CPU row-by-row.  Result: 16/25 pass. The failures are all pre-existing unsupported cuDF types (UUID, STRUCT, BLOB, small DECIMAL, TIMESTAMP_TZ) plus one empty-table edge case and one decimal sort bug — none introduced by this PR.  *Apache Iceberg REST Catalog* — tested against the official `apache/iceberg-rest-fixture` Docker image. Created tables with interleaved INSERT + DELETE operations through the REST API. Verified GPU matches CPU for tables with positional deletes across multiple snapshots.  **Targeted Correctness Checks**  - Equality deletes on Spark-generated tables (deflate manifests, heterogeneous schemas): GPU = CPU - Schema evolution (column added across snapshots, NULL injection): GPU = CPU - Snapshot time-travel (snap1 = no deletes, snap2 = with deletes): GPU = CPU - Insert-delete-insert pattern (reinserted rows survive older deletes): GPU = CPU - `GROUP BY` on evolved partition column (column is data in some files, partition in others): GPU = CPU - Realistic benchmark (1M rows, 10 data files, 20 equality delete rounds, compound key): GPU = CPU = 997,865 rows  --- ## Known Limitations  - **Column rename:** `ALTER TABLE RENAME COLUMN` is not supported. Iceberg tracks renames via field IDs, but cuDF's parquet reader doesn't expose Thrift-level field IDs from the parquet footer. The fix is implemented (field-ID resolution in `detect_columns_and_apply_projection`) but blocked on cuDF metadata access. I might be wrong on this one and need to reinvestigate this deeper - **Type widening:** `ALTER TABLE ALTER COLUMN x SET DATA TYPE BIGINT` (was `INT`) causes a cuDF \"Type mismatch in columns to concatenate\" error because old files have `INT32` and new files have `INT64`. Needs a post-read cast step and might affect other downstream. - **`count(*)` with equality deletes:** DuckDB optimizes `count(*)` to read row counts from parquet footer metadata, bypassing our delete filter entirely. I ahve submitted a PR on creating a cpu_source_task for these types of operation. - **Unsupported cuDF types:** UUID, STRUCT, BLOB, small DECIMAL, TIMESTAMP_TZ — these fail on any scan (parquet or Iceberg), not Iceberg-specific.  Co-authored-by: Claude Opus 4.7 (1M context) <noreply@anthropic.com>") | 3 months agoApr 27, 2026 |
| [third\_party](https://github.com/sirius-db/sirius/tree/dev/third_party "third_party") | [third\_party](https://github.com/sirius-db/sirius/tree/dev/third_party "third_party") | [test(s3): self-managing MinIO via testcontainers; retire manual docke…](https://github.com/sirius-db/sirius/commit/037170225b79d6618d5d86907610ed7c9b23b673 "test(s3): self-managing MinIO via testcontainers; retire manual docker setup (#906)  > **Note:** this PR was prepared by an AI agent (Claude) working with @mbrobbel.  ## Why  In preparation for https://github.com/sirius-db/sirius/issues/904.  The `[s3]` integration tests (S3 I/O, SigV4, scan-manager, SQL-over-S3) need a MinIO backend, and today that backend is set up **out of band** in a way that's easy to get wrong:  - `make s3-up` runs `ensure_tls_certs.sh` → `docker compose up` (two MinIO services on fixed ports 9000/9443) → `fixtures.sh` (uploads fixtures via a `minio/mc` container). - You then `source env.sh` and run the Catch2 binary, and must remember `make s3-down` afterwards.  So the tests depend on a container already running on fixed ports (collisions / leftover state across runs), require a manual pre-step **and** manual cleanup, and silently skip if you forget to source the env. In practice this friction means the suite is rarely run end-to-end.  ## What this changes  Move the whole setup **into the test binary** so running the `[s3]` suite needs no external command and no pre-existing state:  - With `SIRIUS_TEST_S3_AUTO=1`, the binary starts HTTP + self-signed-TLS MinIO on **dynamically-mapped** ports, generates fixtures, uploads them, runs the tests, and tears the containers down on exit (testcontainers' Ryuk also cleans up on a crash). - Fixtures are uploaded **from the host** using Sirius's own SigV4 signer (`sign_request`) + libcurl — so no `minio/mc` container and no docker network are needed. - The harness publishes the existing `SIRIUS_TEST_S3_*` env contract, so **no test files change** — only the harness, the build, and the Makefile. - It's opt-in (`SIRIUS_TEST_S3_AUTO`), so the default `make test` path stays Docker-free; bring-up happens eagerly in `unittest.cpp` `main()` so a strict-mode failure exits non-zero instead of silently skipping.  Net result: `make s3-test` is now one self-contained command, and `s3-up`/`s3-down`, `docker-compose.yml`, `env.sh`, `fixtures.sh`, and `ensure_tls_certs.sh` are gone.  ## How MinIO is launched  C++ has no first-class Testcontainers library, so we use the official **testcontainers-native** C bridge — but its published prototype can't set a container command (MinIO takes its mode from `server /data …`, with no env-var equivalent). We **don't vendor** it: CMake fetches it at a pinned commit at configure time and applies a small in-repo patch (`third_party/testcontainers-native.patch`) that adds:  - `tc_container_with_cmd` — container command support; - real error propagation from `tc_container_run` (upstream dropped it); - a fix for an upstream double-append bug in the container registry.  Dropping `mc` (host-side upload instead) removed the only need for container networking, so the patch stays command-only.  ## Build impact  Building the unit tests now requires a **Go toolchain** (added to pixi as `go-cgo`) and **network access at configure time** (to clone testcontainers-native). This applies to every build, including CI — there is intentionally no opt-out flag.  ## Testing  Verified on a Blackwell GPU host:  - `[s3][integration]~[large]~[aws]~[gpu_execution]` → **82 cases pass** - `[s3]~[integration]~[large]~[aws]` (incl. HTTP + TLS) → **59 cases pass**  `make s3-test` excludes `[gpu_execution]`: those small-budget S3 SQL tests hardcode a 256 MiB GPU limit with no disk spill, which loops in the downgrade executor on large-VRAM GPUs (RMM pool sizing). That is a **pre-existing** issue unrelated to this change, left excluded with an explanatory comment.  ## Commits  1. **Build patched testcontainers-native from upstream** — FetchContent at a pinned commit + the in-repo patch; nothing upstream is vendored. 2. **Add the testcontainers-backed MinIO harness** — `test/cpp/utils/s3_container.*` + build wiring. 3. **Drive `[s3]` tests via the harness; retire the docker/shell glue** — Makefile targets, deleted scripts, docs.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") | 3 weeks agoJun 10, 2026 |
| [tools](https://github.com/sirius-db/sirius/tree/dev/tools "tools") | [tools](https://github.com/sirius-db/sirius/tree/dev/tools "tools") | [Implements using rebind\_stream (](https://github.com/sirius-db/sirius/commit/a258ab021297722b58a724fef099a6ade8f6d718 "Implements using rebind_stream (#918)  Currently all deallocations happen on the stream on which they allocations were made. This PR adds an API which allows for changing that stream, so that deallocations can be made to happen on the active stream. This API is used when converting a data_batch from GPU to Host so that the deallocations happen on the downgrade executor's stream and not block other task execution streams. It also uses the rebind_stream API when preparing the data for a task to be executed. That way any input data that is freed, gets deallocated there as well, minimizing peak memory consumption.  This PR depends on https://github.com/NVIDIA/cuCascade/pull/146  This PR additionally ensures that only one downgrade executor monitor request can exist at a time. This prevents from too many back to back requests.  It also improves logging of memory state at the beginning and end of a query so that we can monitor and check for memory leaks.") [#918](https://github.com/sirius-db/sirius/pull/918) [)](https://github.com/sirius-db/sirius/commit/a258ab021297722b58a724fef099a6ade8f6d718 "Implements using rebind_stream (#918)  Currently all deallocations happen on the stream on which they allocations were made. This PR adds an API which allows for changing that stream, so that deallocations can be made to happen on the active stream. This API is used when converting a data_batch from GPU to Host so that the deallocations happen on the downgrade executor's stream and not block other task execution streams. It also uses the rebind_stream API when preparing the data for a task to be executed. That way any input data that is freed, gets deallocated there as well, minimizing peak memory consumption.  This PR depends on https://github.com/NVIDIA/cuCascade/pull/146  This PR additionally ensures that only one downgrade executor monitor request can exist at a time. This prevents from too many back to back requests.  It also improves logging of memory state at the beginning and end of a query so that we can monitor and check for memory leaks.") | 3 weeks agoJun 11, 2026 |
| [utils/stream\_check](https://github.com/sirius-db/sirius/tree/dev/utils/stream_check "This path skips through empty directories") | [utils/stream\_check](https://github.com/sirius-db/sirius/tree/dev/utils/stream_check "This path skips through empty directories") | [Feature/add stream to tasks (](https://github.com/sirius-db/sirius/commit/4685c0e6c4dbbe5f7175a3faffe129b4d08796b4 "Feature/add stream to tasks (#214)  * (+) don't rely on exceptions to check for missing optional configs  * (+) bug fix in configs, allowing predicates for config with optional and variants and iterables  * acquire stream in gpu pieline executor  * refactor cache mane  * acquire a stream in executor, pass it to execute  * change lock location to /home/amin/.sirisu  * linter fix  * remvoe the lock after creation  * remove extenion file  * remove me  * working on sirisu_physlca_ungrouped_aggregate to make it work with stream and memory_resource  * (+) remove all the default initilzied stream variables, forcing user to pass a stream  * (+) added stream chcker library to ldopen the suymbols and log the use of cudf::get_default_stream  * linter fixes") [#214](https://github.com/sirius-db/sirius/pull/214) [)](https://github.com/sirius-db/sirius/commit/4685c0e6c4dbbe5f7175a3faffe129b4d08796b4 "Feature/add stream to tasks (#214)  * (+) don't rely on exceptions to check for missing optional configs  * (+) bug fix in configs, allowing predicates for config with optional and variants and iterables  * acquire stream in gpu pieline executor  * refactor cache mane  * acquire a stream in executor, pass it to execute  * change lock location to /home/amin/.sirisu  * linter fix  * remvoe the lock after creation  * remove extenion file  * remove me  * working on sirisu_physlca_ungrouped_aggregate to make it work with stream and memory_resource  * (+) remove all the default initilzied stream variables, forcing user to pass a stream  * (+) added stream chcker library to ldopen the suymbols and log the use of cudf::get_default_stream  * linter fixes") | 5 months agoFeb 9, 2026 |
| [vcpkg @ ffc071e](https://github.com/Microsoft/vcpkg/tree/ffc071e0c08432c60c9b64f00334c0227667931b "vcpkg") | [vcpkg @ ffc071e](https://github.com/Microsoft/vcpkg/tree/ffc071e0c08432c60c9b64f00334c0227667931b "vcpkg") | [Add](https://github.com/sirius-db/sirius/commit/d40e8ce9d605d8c0a9118159bc34cf6c234e6abe "Add `vcpkg` build support (#366)  Closes #547.  Adds `vcpkg` build support to build a fully statically linked extension for distribution: ``` ldd build/vcpkg-release/extension/sirius/sirius.duckdb_extension           linux-vdso.so.1 (0x00007fb1b8676000)           libdl.so.2 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libdl.so.2 (0x00007fb1a37f9000)           librt.so.1 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/librt.so.1 (0x00007fb1a37f4000)           libpthread.so.0 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libpthread.so.0 (0x00007fb1a37ef000)           libstdc++.so.6 => not found           libm.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libm.so.6 (0x00007fb1a36f7000)           libgcc_s.so.1 => /home/matthijs/sirius-vcpkg/.pixi/envs/vcpkg/lib/libgcc_s.so.1 (0x00007fb1a36c8000)           libc.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libc.so.6 (0x00007fb1a3000000)           /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib64/ld-linux-x86-64.so.2 (0x00007fb1b8678000) ```  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>")`vcpkg` [build support (](https://github.com/sirius-db/sirius/commit/d40e8ce9d605d8c0a9118159bc34cf6c234e6abe "Add `vcpkg` build support (#366)  Closes #547.  Adds `vcpkg` build support to build a fully statically linked extension for distribution: ``` ldd build/vcpkg-release/extension/sirius/sirius.duckdb_extension           linux-vdso.so.1 (0x00007fb1b8676000)           libdl.so.2 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libdl.so.2 (0x00007fb1a37f9000)           librt.so.1 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/librt.so.1 (0x00007fb1a37f4000)           libpthread.so.0 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libpthread.so.0 (0x00007fb1a37ef000)           libstdc++.so.6 => not found           libm.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libm.so.6 (0x00007fb1a36f7000)           libgcc_s.so.1 => /home/matthijs/sirius-vcpkg/.pixi/envs/vcpkg/lib/libgcc_s.so.1 (0x00007fb1a36c8000)           libc.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libc.so.6 (0x00007fb1a3000000)           /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib64/ld-linux-x86-64.so.2 (0x00007fb1b8678000) ```  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") [#366](https://github.com/sirius-db/sirius/pull/366) [)](https://github.com/sirius-db/sirius/commit/d40e8ce9d605d8c0a9118159bc34cf6c234e6abe "Add `vcpkg` build support (#366)  Closes #547.  Adds `vcpkg` build support to build a fully statically linked extension for distribution: ``` ldd build/vcpkg-release/extension/sirius/sirius.duckdb_extension           linux-vdso.so.1 (0x00007fb1b8676000)           libdl.so.2 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libdl.so.2 (0x00007fb1a37f9000)           librt.so.1 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/librt.so.1 (0x00007fb1a37f4000)           libpthread.so.0 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libpthread.so.0 (0x00007fb1a37ef000)           libstdc++.so.6 => not found           libm.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libm.so.6 (0x00007fb1a36f7000)           libgcc_s.so.1 => /home/matthijs/sirius-vcpkg/.pixi/envs/vcpkg/lib/libgcc_s.so.1 (0x00007fb1a36c8000)           libc.so.6 => /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib/libc.so.6 (0x00007fb1a3000000)           /nix/store/l0l2ll1lmylczj1ihqn351af2kyp5x19-glibc-2.42-51/lib64/ld-linux-x86-64.so.2 (0x00007fb1b8678000) ```  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") | 3 months agoApr 10, 2026 |
| [vcpkg\_ports](https://github.com/sirius-db/sirius/tree/dev/vcpkg_ports "vcpkg_ports") | [vcpkg\_ports](https://github.com/sirius-db/sirius/tree/dev/vcpkg_ports "vcpkg_ports") | [build: drop obsolete CMake/vcpkg workarounds (](https://github.com/sirius-db/sirius/commit/402a4e44b3c8922ddd815171a3e679f95bbb5299 "build: drop obsolete CMake/vcpkg workarounds (#1041)  Removes build workarounds that are no longer needed after dependency bumps (RAPIDS 26.06, cuCascade at the current submodule pointer). Each change was verified with a from-source vcpkg build that relinks and runs the extension on GPU.  - **NUMA shim + duplicate yaml-cpp link** (`CMakeLists.txt`): cuCascade now resolves libnuma via its bundled `FindNuma` (`Numa::Numa`), so the bare `-lnuma` imported-target shim is dead; yaml-cpp was already linked to both extension targets in the shared block. - **cuDF Arrow dependency + DLPack patch**: cuDF 26.06 moved interop to nanoarrow and no longer `find_package(Arrow)` (drops building Arrow + its subtree); the DLPack patch targeted the old single-arg `find_and_configure_dlpack` call and no longer matched, so it was inert. - **cuDF GTest fetch**: `CUDF_BUILD_TESTUTIL=OFF` gates out the test utilities that were the only thing pulling GTest. - **rmm cudart rewrite + CUDA flag**: rmm 26.06 exports `CUDA::cudart_static` directly (rewrite matched nothing); rmm is CXX-only so `-DCMAKE_CUDA_FLAGS` was reported unused. - **nvtx3 hand-written config**: use upstream install rules (`NVTX3_INSTALL`), which provide the exported targets and a correct version file (the hand-written config emitted an empty version).  Kept (verified still required): the `CUDA::nvml_static`→shared-stub redirect, the conda-CCCL strip + include ordering, static-cudart preference, the hand-written cccl port (upstream install rules break vcpkg's `share/` config layout), and the kvikio cuFile force-disable (still needed for toolkits whose cuFile lacks/omits the batch/stream API, e.g. the cuda12 CI image).  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") [#1041](https://github.com/sirius-db/sirius/pull/1041) [)](https://github.com/sirius-db/sirius/commit/402a4e44b3c8922ddd815171a3e679f95bbb5299 "build: drop obsolete CMake/vcpkg workarounds (#1041)  Removes build workarounds that are no longer needed after dependency bumps (RAPIDS 26.06, cuCascade at the current submodule pointer). Each change was verified with a from-source vcpkg build that relinks and runs the extension on GPU.  - **NUMA shim + duplicate yaml-cpp link** (`CMakeLists.txt`): cuCascade now resolves libnuma via its bundled `FindNuma` (`Numa::Numa`), so the bare `-lnuma` imported-target shim is dead; yaml-cpp was already linked to both extension targets in the shared block. - **cuDF Arrow dependency + DLPack patch**: cuDF 26.06 moved interop to nanoarrow and no longer `find_package(Arrow)` (drops building Arrow + its subtree); the DLPack patch targeted the old single-arg `find_and_configure_dlpack` call and no longer matched, so it was inert. - **cuDF GTest fetch**: `CUDF_BUILD_TESTUTIL=OFF` gates out the test utilities that were the only thing pulling GTest. - **rmm cudart rewrite + CUDA flag**: rmm 26.06 exports `CUDA::cudart_static` directly (rewrite matched nothing); rmm is CXX-only so `-DCMAKE_CUDA_FLAGS` was reported unused. - **nvtx3 hand-written config**: use upstream install rules (`NVTX3_INSTALL`), which provide the exported targets and a correct version file (the hand-written config emitted an empty version).  Kept (verified still required): the `CUDA::nvml_static`→shared-stub redirect, the conda-CCCL strip + include ordering, static-cudart preference, the hand-written cccl port (upstream install rules break vcpkg's `share/` config layout), and the kvikio cuFile force-disable (still needed for toolkits whose cuFile lacks/omits the batch/stream API, e.g. the cuda12 CI image).  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") | yesterdayJul 1, 2026 |
| [vcpkg\_triplets](https://github.com/sirius-db/sirius/tree/dev/vcpkg_triplets "vcpkg_triplets") | [vcpkg\_triplets](https://github.com/sirius-db/sirius/tree/dev/vcpkg_triplets "vcpkg_triplets") | [Add extension-ci-tools distribution workflow (](https://github.com/sirius-db/sirius/commit/484db3509c395646a7c5cfd0543e860fa2e9cd9b "Add extension-ci-tools distribution workflow (#621)  ## Summary - Switch `extension-ci-tools` submodule to `sirius-db/extension-ci-tools` (`sirius` branch) which adds CUDA toolkit support to Docker build images - Add `ci-release` CMake preset and Makefile target for Docker-based vcpkg builds (ccache, no mold) - Add `distribution.yml` workflow that builds Linux amd64/arm64 extension binaries via the standard DuckDB extension CI infrastructure - Rename vcpkg triplets to `x64-linux-release` / `arm64-linux-release` to match DuckDB's distribution matrix  The `sirius` branch of `extension-ci-tools` adds: - `;cuda;` extra_toolchains option in Docker images (installs nvcc, cudart, nvjitlink, nvrtc from NVIDIA RHEL 8 repos) - Workflow inputs for `vcpkg_overlay_ports`, `vcpkg_overlay_triplets`, `cuda_archs`, `cuda_version`  All fork changes are additive and designed to be upstreamed to `duckdb/extension-ci-tools` once validated.  ## Test plan - [x] Distribution workflow triggers and Docker image builds with CUDA toolkit - [x] vcpkg resolves cudf + dependencies using overlay ports - [x] CMake finds nvcc and compiles CUDA sources - [x] `.duckdb_extension` artifact is produced and uploaded - [x] Existing `check.yml` and `test.yml` workflows still pass (triplet rename)  🤖 Created by Claude  Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>  ---------  Co-authored-by: Claude Opus 4.6 (1M context) <noreply@anthropic.com>") [#621](https://github.com/sirius-db/sirius/pull/621) [)](https://github.com/sirius-db/sirius/commit/484db3509c395646a7c5cfd0543e860fa2e9cd9b "Add extension-ci-tools distribution workflow (#621)  ## Summary - Switch `extension-ci-tools` submodule to `sirius-db/extension-ci-tools` (`sirius` branch) which adds CUDA toolkit support to Docker build images - Add `ci-release` CMake preset and Makefile target for Docker-based vcpkg builds (ccache, no mold) - Add `distribution.yml` workflow that builds Linux amd64/arm64 extension binaries via the standard DuckDB extension CI infrastructure - Rename vcpkg triplets to `x64-linux-release` / `arm64-linux-release` to match DuckDB's distribution matrix  The `sirius` branch of `extension-ci-tools` adds: - `;cuda;` extra_toolchains option in Docker images (installs nvcc, cudart, nvjitlink, nvrtc from NVIDIA RHEL 8 repos) - Workflow inputs for `vcpkg_overlay_ports`, `vcpkg_overlay_triplets`, `cuda_archs`, `cuda_version`  All fork changes are additive and designed to be upstreamed to `duckdb/extension-ci-tools` once validated.  ## Test plan - [x] Distribution workflow triggers and Docker image builds with CUDA toolkit - [x] vcpkg resolves cudf + dependencies using overlay ports - [x] CMake finds nvcc and compiles CUDA sources - [x] `.duckdb_extension` artifact is produced and uploaded - [x] Existing `check.yml` and `test.yml` workflows still pass (triplet rename)  🤖 Created by Claude  Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>  ---------  Co-authored-by: Claude Opus 4.6 (1M context) <noreply@anthropic.com>") | 3 months agoApr 20, 2026 |
| [.clang-format](https://github.com/sirius-db/sirius/blob/dev/.clang-format ".clang-format") | [.clang-format](https://github.com/sirius-db/sirius/blob/dev/.clang-format ".clang-format") | [Feature: Enabling pre-commit for sirius-db (](https://github.com/sirius-db/sirius/commit/75ad0a274150deb338c77760999993b20a0bcff7 "Feature: Enabling pre-commit for sirius-db (#125)  * (+) added pre-commit config  * add pre-commit to the dependecies  * running pre-commit on the repo  * enable it on pull-request  * add ans to dictionary") [#125](https://github.com/sirius-db/sirius/pull/125) [)](https://github.com/sirius-db/sirius/commit/75ad0a274150deb338c77760999993b20a0bcff7 "Feature: Enabling pre-commit for sirius-db (#125)  * (+) added pre-commit config  * add pre-commit to the dependecies  * running pre-commit on the repo  * enable it on pull-request  * add ans to dictionary") | 7 months agoDec 15, 2025 |
| [.clang-tidy](https://github.com/sirius-db/sirius/blob/dev/.clang-tidy ".clang-tidy") | [.clang-tidy](https://github.com/sirius-db/sirius/blob/dev/.clang-tidy ".clang-tidy") | [Add tidy checks to help avoid deep copies (](https://github.com/sirius-db/sirius/commit/8b9a25e4c6e060ca2791ab2cc49a4a8a4ad775d4 "Add tidy checks to help avoid deep copies (#416)") [#416](https://github.com/sirius-db/sirius/pull/416) [)](https://github.com/sirius-db/sirius/commit/8b9a25e4c6e060ca2791ab2cc49a4a8a4ad775d4 "Add tidy checks to help avoid deep copies (#416)") | 4 months agoMar 9, 2026 |
| [.clangd](https://github.com/sirius-db/sirius/blob/dev/.clangd ".clangd") | [.clangd](https://github.com/sirius-db/sirius/blob/dev/.clangd ".clangd") | [fix: clangd configuration for pixi environment (](https://github.com/sirius-db/sirius/commit/995806b3481d905b9ac414bdc3f89de5bb2a946b "fix: clangd configuration for pixi environment (#359)  * Fix clangd: pin clang to match clang-tools, move CUDA flags to conditional block  Three issues with the clangd configuration:  1. clang (>=21) resolved to v22 while clang-tools (v21) provides clangd v21.    clangd v21 expects lib/clang/21/include/ (for stddef.h etc.) but only    lib/clang/22/ exists, breaking all standard library resolution. Pin clang    to <22 until clang-tools v22 is available on conda-forge.  2. -x cuda was applied globally to all files, forcing clangd to treat every    .cpp as CUDA code. Move it into the CUDA-specific PathMatch block.  3. Compiler was hardcoded to clang++-18 which doesn't exist in pixi (pixi    provides clang++). Also strip -fmodules-ts and related C++20 module flags    that clangd doesn't support.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove unnecessary flags  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") [#359](https://github.com/sirius-db/sirius/pull/359) [)](https://github.com/sirius-db/sirius/commit/995806b3481d905b9ac414bdc3f89de5bb2a946b "fix: clangd configuration for pixi environment (#359)  * Fix clangd: pin clang to match clang-tools, move CUDA flags to conditional block  Three issues with the clangd configuration:  1. clang (>=21) resolved to v22 while clang-tools (v21) provides clangd v21.    clangd v21 expects lib/clang/21/include/ (for stddef.h etc.) but only    lib/clang/22/ exists, breaking all standard library resolution. Pin clang    to <22 until clang-tools v22 is available on conda-forge.  2. -x cuda was applied globally to all files, forcing clangd to treat every    .cpp as CUDA code. Move it into the CUDA-specific PathMatch block.  3. Compiler was hardcoded to clang++-18 which doesn't exist in pixi (pixi    provides clang++). Also strip -fmodules-ts and related C++20 module flags    that clangd doesn't support.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove unnecessary flags  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") | 5 months agoFeb 27, 2026 |
| [.codespell\_words](https://github.com/sirius-db/sirius/blob/dev/.codespell_words ".codespell_words") | [.codespell\_words](https://github.com/sirius-db/sirius/blob/dev/.codespell_words ".codespell_words") | [Support dynamic table filters (](https://github.com/sirius-db/sirius/commit/35c560e79437b3427044764fe08142f87bc7a246 "Support dynamic table filters (#664)  ## Summary Scaffolds the dynamic-filter framework (polymorphic filter base, capability mixin for AST lowering, thread-safe channel, AST-merge helper). No producers or consumers are wired in — runtime behavior is unchanged.  Design and phasing: `docs/super-sirius/dynamic-filters.md`.  ## What's in the diff - `src/include/op/sirius_dynamic_filter.hpp` / `src/op/sirius_dynamic_filter.cpp`   — `sirius_dynamic_filter` base, `sirius_ast_lowerable` mixin,   `sirius_dynamic_zone_map_filter`, `sirius_dynamic_filter_set`,   `merge_ast_dynamic_filters_into_tree`. - `test/cpp/operator/test_sirius_dynamic_filter.cpp` — 16 tests covering channel mechanics, zone-map AST construction, merge behavior, capability   dispatch, and thread-safety. - `docs/super-sirius/dynamic-filters.md` — framework design + 4-phase plan. - `CMakeLists.txt` — register the new source and test.  ## Why this is safe No existing operator or planner is touched. Every symbol added is unreferenced by runtime code paths. Phase 1.1 (follow-up PR) wires it into hash-join build and parquet scan; this PR cannot regress any existing query.") [#664](https://github.com/sirius-db/sirius/pull/664) [)](https://github.com/sirius-db/sirius/commit/35c560e79437b3427044764fe08142f87bc7a246 "Support dynamic table filters (#664)  ## Summary Scaffolds the dynamic-filter framework (polymorphic filter base, capability mixin for AST lowering, thread-safe channel, AST-merge helper). No producers or consumers are wired in — runtime behavior is unchanged.  Design and phasing: `docs/super-sirius/dynamic-filters.md`.  ## What's in the diff - `src/include/op/sirius_dynamic_filter.hpp` / `src/op/sirius_dynamic_filter.cpp`   — `sirius_dynamic_filter` base, `sirius_ast_lowerable` mixin,   `sirius_dynamic_zone_map_filter`, `sirius_dynamic_filter_set`,   `merge_ast_dynamic_filters_into_tree`. - `test/cpp/operator/test_sirius_dynamic_filter.cpp` — 16 tests covering channel mechanics, zone-map AST construction, merge behavior, capability   dispatch, and thread-safety. - `docs/super-sirius/dynamic-filters.md` — framework design + 4-phase plan. - `CMakeLists.txt` — register the new source and test.  ## Why this is safe No existing operator or planner is touched. Every symbol added is unreferenced by runtime code paths. Phase 1.1 (follow-up PR) wires it into hash-join build and parquet scan; this PR cannot regress any existing query.") | 2 months agoMay 18, 2026 |
| [.gitattributes](https://github.com/sirius-db/sirius/blob/dev/.gitattributes ".gitattributes") | [.gitattributes](https://github.com/sirius-db/sirius/blob/dev/.gitattributes ".gitattributes") | [Add Pixi manifest (](https://github.com/sirius-db/sirius/commit/b689db41fc488de01837ec9eee8ae702babfba6f "Add Pixi manifest (#107)  * Add Pixi manifest  * Revert default CMAKE_CUDA_ARCHITECTURES in CMake to native, override in pixi env  * Set `CUDAARCH` via env in test workflow  * Fix typo") [#107](https://github.com/sirius-db/sirius/pull/107) [)](https://github.com/sirius-db/sirius/commit/b689db41fc488de01837ec9eee8ae702babfba6f "Add Pixi manifest (#107)  * Add Pixi manifest  * Revert default CMAKE_CUDA_ARCHITECTURES in CMake to native, override in pixi env  * Set `CUDAARCH` via env in test workflow  * Fix typo") | 8 months agoNov 24, 2025 |
| [.gitignore](https://github.com/sirius-db/sirius/blob/dev/.gitignore ".gitignore") | [.gitignore](https://github.com/sirius-db/sirius/blob/dev/.gitignore ".gitignore") | [Add real-AWS s3-bench test case for future EC2+S3 testing (](https://github.com/sirius-db/sirius/commit/cdfb7d8f9b9cb303401aa99c4fc418a18e290a33 "Add real-AWS s3-bench test case for future EC2+S3 testing (#1049)  ## Summary  This PR restores the manual real-AWS gates that were lost in the REST I/O cutover (#997/#1042), now running against `rest_ioctx`, and adds a connection-scaling benchmark that answers a concrete tuning question: **does raising `max_connections` raise S3 bandwidth on a high-latency path?** (Yes — ~10×; data below.)  ## What's in  - **Live SQL correctness** (`make s3-test-aws-sigv4`): `read_parquet('s3://…')` row-level equality against a local oracle plus aggregates, in **both** signing modes (presigned URL and header SigV4), through the normal `s3://` → `rest_ioctx` routing. Verified green against real AWS us-east-2 (306 assertions) — this exercises real TLS, redirects, clock and STS-token behavior that MinIO cannot. - **AWS bench scenarios** (`SIRIUS_BENCH_BACKEND=aws-s3 make s3-bench`): `aws_https_projected` / `aws_https_full` over SF1 lineitem, each at `max_connections ∈ {1, 32}`, emitting the same perf JSON schema as the MinIO bench with `backend: rest_aws`. `SIRIUS_BENCH_S3_KEY` overrides the object key so an in-region runner can point at SF10. - **Recording plumbing**: per-backend baseline files (`perf-baseline-{minio,aws}.json`, host-keyed, gitignored), a looser AWS WARN threshold (WAN jitter), and per-run history appended to `perf-history-aws.jsonl` for cross-session trend analysis. - **Env guards** with unit tests that run without any AWS access (`[s3][aws][env]`). - Makefile: `s3-bench` selects by `SIRIUS_BENCH_BACKEND` (aws-s3 → `[s3][bench][aws]`, never starts MinIO); the correctness gates exclude `[bench]`.  ## Perf results (real AWS, us-east-2, SF1 lineitem 205.7 MB, median of 2 steady runs)  | scenario | max_connections | wall | footer_fetch | scan | effective throughput | |---|---|---|---|---|---| | projected (9.4 MB) | 1 | 12.9 s | 1.54 s | 10.7 s | 0.73 MB/s | | full (205.7 MB) | 1 | 115.7 s | 1.72 s | 113.2 s | 1.78 MB/s | | projected (9.4 MB) | 32 | 5.3 s | 1.89 s | 2.5 s | 1.92 MB/s | | full (205.7 MB) | 32 | **11.8 s** | 1.93 s | 8.5 s | **17.5 MB/s** |  MinIO loopback reference (same backend/instrumentation, SF10): footer 2.3–8.8 **ms**, full-scan 967 MB/s (HTTP) / 784 MB/s (HTTPS) at mc=32.  ### Analysis  1. **Connection scaling is the bandwidth lever on high-latency paths: mc=1 → 32 = ~10× wall and ~10× effective throughput** (scan-phase-only ratio 12.9–13.6×). A single TCP/TLS stream is bounded by `window / RTT` — on this path one connection is pinned at ~1.8 MB/s regardless of server capacity; 32 parallel streams lift the aggregate to 17.5 MB/s. Scaling is sub-linear (10× for 32×) because the path's aggregate capacity becomes the new ceiling (per-connection efficiency drops 1.78 → ~0.55 MB/s). In effect: `throughput ≈ min(path_capacity, N × window/RTT)`. 2. **Connections do not buy latency**: `footer_fetch` is flat across mc=1/32 in every run (~1.5–1.9 s here vs 2.3–8.8 ms on loopback, ~500–800×) — the footer is one ranged GET, one round-trip, and parallelism cannot shorten a round-trip. Small/projected reads are therefore bind-dominated on WAN (projected@mc=32: 5.3 s wall, only 2.5 s scanning); the lever there is footer/metadata reuse, and the bind cost is per-file, so it multiplies with file count. 3. **On loopback the same knob is not the lever**: RTT ≈ 0 means a single stream can already saturate; bandwidth is bound by TLS/CPU (967 HTTP vs 784 HTTPS = the per-byte TLS cost). `max_connections=32` as the default is right for WAN and harmless locally. 4. **Invariants held in every cell of both columns**: `device_stream_sync_total == 0` (the GPU async-copy path never inline-syncs, latency-regime-independent), payload bytes byte-identical across runs, `retries_total == 0`, `terminal_failures_total == 0`. 5. Same-path comparison vs the pre-cutover async backend (measured 2026-06-22): footer 1.5–1.9 s (was 2.4–4.4 s), steady throughput 17.5 MB/s (was 10.4–11.7) — the REST backend is same-shape but better on both bind and throughput.") [#1049](https://github.com/sirius-db/sirius/pull/1049) [)](https://github.com/sirius-db/sirius/commit/cdfb7d8f9b9cb303401aa99c4fc418a18e290a33 "Add real-AWS s3-bench test case for future EC2+S3 testing (#1049)  ## Summary  This PR restores the manual real-AWS gates that were lost in the REST I/O cutover (#997/#1042), now running against `rest_ioctx`, and adds a connection-scaling benchmark that answers a concrete tuning question: **does raising `max_connections` raise S3 bandwidth on a high-latency path?** (Yes — ~10×; data below.)  ## What's in  - **Live SQL correctness** (`make s3-test-aws-sigv4`): `read_parquet('s3://…')` row-level equality against a local oracle plus aggregates, in **both** signing modes (presigned URL and header SigV4), through the normal `s3://` → `rest_ioctx` routing. Verified green against real AWS us-east-2 (306 assertions) — this exercises real TLS, redirects, clock and STS-token behavior that MinIO cannot. - **AWS bench scenarios** (`SIRIUS_BENCH_BACKEND=aws-s3 make s3-bench`): `aws_https_projected` / `aws_https_full` over SF1 lineitem, each at `max_connections ∈ {1, 32}`, emitting the same perf JSON schema as the MinIO bench with `backend: rest_aws`. `SIRIUS_BENCH_S3_KEY` overrides the object key so an in-region runner can point at SF10. - **Recording plumbing**: per-backend baseline files (`perf-baseline-{minio,aws}.json`, host-keyed, gitignored), a looser AWS WARN threshold (WAN jitter), and per-run history appended to `perf-history-aws.jsonl` for cross-session trend analysis. - **Env guards** with unit tests that run without any AWS access (`[s3][aws][env]`). - Makefile: `s3-bench` selects by `SIRIUS_BENCH_BACKEND` (aws-s3 → `[s3][bench][aws]`, never starts MinIO); the correctness gates exclude `[bench]`.  ## Perf results (real AWS, us-east-2, SF1 lineitem 205.7 MB, median of 2 steady runs)  | scenario | max_connections | wall | footer_fetch | scan | effective throughput | |---|---|---|---|---|---| | projected (9.4 MB) | 1 | 12.9 s | 1.54 s | 10.7 s | 0.73 MB/s | | full (205.7 MB) | 1 | 115.7 s | 1.72 s | 113.2 s | 1.78 MB/s | | projected (9.4 MB) | 32 | 5.3 s | 1.89 s | 2.5 s | 1.92 MB/s | | full (205.7 MB) | 32 | **11.8 s** | 1.93 s | 8.5 s | **17.5 MB/s** |  MinIO loopback reference (same backend/instrumentation, SF10): footer 2.3–8.8 **ms**, full-scan 967 MB/s (HTTP) / 784 MB/s (HTTPS) at mc=32.  ### Analysis  1. **Connection scaling is the bandwidth lever on high-latency paths: mc=1 → 32 = ~10× wall and ~10× effective throughput** (scan-phase-only ratio 12.9–13.6×). A single TCP/TLS stream is bounded by `window / RTT` — on this path one connection is pinned at ~1.8 MB/s regardless of server capacity; 32 parallel streams lift the aggregate to 17.5 MB/s. Scaling is sub-linear (10× for 32×) because the path's aggregate capacity becomes the new ceiling (per-connection efficiency drops 1.78 → ~0.55 MB/s). In effect: `throughput ≈ min(path_capacity, N × window/RTT)`. 2. **Connections do not buy latency**: `footer_fetch` is flat across mc=1/32 in every run (~1.5–1.9 s here vs 2.3–8.8 ms on loopback, ~500–800×) — the footer is one ranged GET, one round-trip, and parallelism cannot shorten a round-trip. Small/projected reads are therefore bind-dominated on WAN (projected@mc=32: 5.3 s wall, only 2.5 s scanning); the lever there is footer/metadata reuse, and the bind cost is per-file, so it multiplies with file count. 3. **On loopback the same knob is not the lever**: RTT ≈ 0 means a single stream can already saturate; bandwidth is bound by TLS/CPU (967 HTTP vs 784 HTTPS = the per-byte TLS cost). `max_connections=32` as the default is right for WAN and harmless locally. 4. **Invariants held in every cell of both columns**: `device_stream_sync_total == 0` (the GPU async-copy path never inline-syncs, latency-regime-independent), payload bytes byte-identical across runs, `retries_total == 0`, `terminal_failures_total == 0`. 5. Same-path comparison vs the pre-cutover async backend (measured 2026-06-22): footer 1.5–1.9 s (was 2.4–4.4 s), steady throughput 17.5 MB/s (was 10.4–11.7) — the REST backend is same-shape but better on both bind and throughput.") | 19 hours agoJul 2, 2026 |
| [.gitmodules](https://github.com/sirius-db/sirius/blob/dev/.gitmodules ".gitmodules") | [.gitmodules](https://github.com/sirius-db/sirius/blob/dev/.gitmodules ".gitmodules") | [feat(starrocks): translate FE plan fragments over BRPC PInternalServi…](https://github.com/sirius-db/sirius/commit/f5b03884d1094202f99a591b87be344c00b71d86 "feat(starrocks): translate FE plan fragments over BRPC PInternalService (#941)  ## Summary  Adds the StarRocks compute-node BRPC `PInternalService` path so a Sirius CN can receive plan fragments dispatched by a StarRocks FE and translate them. Part of #826.  - Implement `exec_plan_fragment` and `exec_batch_plan_fragments`: deserialize the binary-thrift `TExecPlanFragmentParams` attachment and run it through a thrift→Substrait `PlanTranslator`. - Add a Baidu PRPC frame transport (raw-TCP `PRPC` envelope — FE→backend RPC is not gRPC/HTTP2) and a Tower-based BRPC server with tonic-like graceful shutdown. - Generate BRPC and StarRocks protobuf bindings in `build.rs`; vendor `apache/brpc` as a submodule for the upstream proto definitions.  ## Notes  - Translation currently validates the fragment and logs the resulting plan; fragment execution is out of scope for this PR. - All changes are under `experimental/starrocks`. CN registration and FE heartbeat were already wired; this adds the fragment-handling RPC surface on top.  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") | 2 weeks agoJun 16, 2026 |
| [.pre-commit-config.yaml](https://github.com/sirius-db/sirius/blob/dev/.pre-commit-config.yaml ".pre-commit-config.yaml") | [.pre-commit-config.yaml](https://github.com/sirius-db/sirius/blob/dev/.pre-commit-config.yaml ".pre-commit-config.yaml") | [S3 change to align with scan-manager cleanup (](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") [#913](https://github.com/sirius-db/sirius/pull/913) [) (](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") [#923](https://github.com/sirius-db/sirius/pull/923) [)](https://github.com/sirius-db/sirius/commit/03ae71c5eff9659ddb81fbcb0c77433a202fa659 "S3 change to align with scan-manager cleanup (#913)  (#923)  With scan-manager cleanup (#913) merged, Running the full `[s3]` suite against the merged result surfaced a handful of regressions and coverage gaps, all fixed here. And align comments with current code.  ### Crash fixes  - **`make s3-test` died at 93%**: `sirius_scan_manager::create_datasource` was `noexcept` but performs an S3 HEAD via `create_io_object`, so a missing key (HTTP 404) terminated the whole process instead of surfacing a query error. Dropped the `noexcept` and documented the throwing contract; the existing \"describe_parquet surfaces S3 missing-object errors cleanly\" test covers it. - **`make s3-test-large` SIGSEGV** in the S3 async backend's CUDA copy callback: `cuda_copy_done` touched the transfer and the reactor after the `cuda_done` release-store — the store is what licenses the reactor thread to delete the transfer (and, via shutdown, tear down the curl multi handle). The release-store is now the callback's last access; the wakeup fires before it, while `_copying` membership still pins both lifetimes. The async device-read benchmark is unaffected (~10x blocking at 16 connections, `stream_syncs=0`).  ### Restored behaviors  - **`use_sirius_datasource=false` (local cudf/KvikIO fallback)** was a no-op: the local uring claimed local paths unconditionally. The flag is now honored at the routing level in `create_datasource` — unclaimed local slices carry no datasource and materialize falls back to `cudf::io::datasource::create`. The uring itself is still always constructed (the DuckDB-native GPU scan needs it for host reads), and object-store paths are unaffected: **S3 remains GPU-only with no CPU fallback**. Explicit `file://` URIs behave exactly like bare local paths on every fallback site. - **Chunk prewarm was inert**: `enable_chunk_prewarm` survived but no call site inserted ranges into the prefetch cache. `parquet_gpu_ingestible::run_batch` now prewarms the selected row groups' merged column-chunk byte ranges (plus parquet magic + footer) once projection and row-group pruning are final; `describe_parquet()` stays metadata-only.  ### Test coverage  - The S3 scan-manager coverage dropped by the cleanup (two test files were edited but removed from `TEST_SOURCES`, so they never compiled) is selectively migrated onto the `create_datasource` seam: backend selection (`s3_use_async_backend` → async vs blocking), S3 datasource routing (an `s3://` slice always carries a non-null datasource, including with `use_sirius_datasource=false`), local fallback (bare path and `file://`), scan-side prewarm on/off, and a routing spy proving slice reads flow through the Sirius datasource rather than cudf's bundled fallback. The two orphaned files are deleted; everything bound to the removed APIs was rewritten, not ported. - A nested struct/list/map S3-vs-local-oracle probe is included; it currently exercises the clean bind-time rejection (`[parquet_helpers] nested parquet types not yet supported in sirius_read_parquet`) and will flip to the oracle comparison when nested support lands. - **New configure-time orphan-test guard**: a `test_*.cpp` present in the tree but missing from `TEST_SOURCES` now fails the configure (this class of silent coverage loss is what allowed the gap above). Eleven pre-existing orphans are allowlisted with reasons in `test/cpp/ORPHAN_ALLOWLIST.cmake` for their owners to triage — the list is meant to only ever shrink.  ### Comment accuracy  Ownership and naming comments that still described the pre-cleanup world (SiriusContext owning the backends, `parquet_split_provider`/`cached_split_provider`/`io_ctx_for` references, \"reserved\" uring knobs that are consumed again) now describe the merged architecture.") | 3 weeks agoJun 12, 2026 |
| [AGENTS.md](https://github.com/sirius-db/sirius/blob/dev/AGENTS.md "AGENTS.md") | [AGENTS.md](https://github.com/sirius-db/sirius/blob/dev/AGENTS.md "AGENTS.md") | [Add Codex AGENTS.md entrypoint (](https://github.com/sirius-db/sirius/commit/23c3f72b53cb69c2a0b9c65640c7e5bbcf8e7e04 "Add Codex AGENTS.md entrypoint (#875)  ## Summary - Add a root `AGENTS.md` file as the Codex-compatible repository instruction entrypoint. - Delegate Codex guidance to the existing `CLAUDE.md` so the project does not maintain duplicate AI assistant instructions. - Stop ignoring `AGENTS.md` and ignore `AGENTS.override.md` instead.  ## Why Codex discovers repository guidance from `AGENTS.md` by default. Committing this file makes Codex users load the same project instructions that are already maintained in `CLAUDE.md`, keeping shared expectations versioned and reducing drift between assistant-specific instruction files.  ## Local overrides Codex checks `AGENTS.override.md` before `AGENTS.md` in a directory and uses at most one instruction file per directory. Because `AGENTS.override.md` is ignored, contributors can create it for temporary or machine-local Codex guidance without accidentally committing those overrides.  If a local override should keep the shared repo guidance, include an explicit delegation such as:  ```md Read `CLAUDE.md` and follow its instructions.  Additional local instructions go here. ```  ## Testing - Verified `AGENTS.md` is no longer ignored by Git. - Verified `AGENTS.override.md` is ignored by Git.") [#875](https://github.com/sirius-db/sirius/pull/875) [)](https://github.com/sirius-db/sirius/commit/23c3f72b53cb69c2a0b9c65640c7e5bbcf8e7e04 "Add Codex AGENTS.md entrypoint (#875)  ## Summary - Add a root `AGENTS.md` file as the Codex-compatible repository instruction entrypoint. - Delegate Codex guidance to the existing `CLAUDE.md` so the project does not maintain duplicate AI assistant instructions. - Stop ignoring `AGENTS.md` and ignore `AGENTS.override.md` instead.  ## Why Codex discovers repository guidance from `AGENTS.md` by default. Committing this file makes Codex users load the same project instructions that are already maintained in `CLAUDE.md`, keeping shared expectations versioned and reducing drift between assistant-specific instruction files.  ## Local overrides Codex checks `AGENTS.override.md` before `AGENTS.md` in a directory and uses at most one instruction file per directory. Because `AGENTS.override.md` is ignored, contributors can create it for temporary or machine-local Codex guidance without accidentally committing those overrides.  If a local override should keep the shared repo guidance, include an explicit delegation such as:  ```md Read `CLAUDE.md` and follow its instructions.  Additional local instructions go here. ```  ## Testing - Verified `AGENTS.md` is no longer ignored by Git. - Verified `AGENTS.override.md` is ignored by Git.") | last monthJun 4, 2026 |
| [CLAUDE.md](https://github.com/sirius-db/sirius/blob/dev/CLAUDE.md "CLAUDE.md") | [CLAUDE.md](https://github.com/sirius-db/sirius/blob/dev/CLAUDE.md "CLAUDE.md") | [docs: trim CLAUDE.md to non-obvious, behavior-changing guidance (](https://github.com/sirius-db/sirius/commit/24f7461ef785794e2c993e125faa74a94c52800c "docs: trim CLAUDE.md to non-obvious, behavior-changing guidance (#874)  ## Trim `CLAUDE.md` to non-obvious, behavior-changing guidance  `CLAUDE.md` has grown organically as the project has — which is natural, and it means it now covers a lot of ground. This PR is a focused tidy-up. The goal is to make it as useful as possible for Claude Code by keeping it tight and current.  The thing that makes `CLAUDE.md` different from a README: it's **injected into the context of every Claude Code session in this repo**. A human opens a README when they want it; the model reads `CLAUDE.md` on *every* task. That changes the cost/benefit. Each line is a small recurring cost, and — more importantly — the fewer lines there are, the more attention lands on the rules that really matter. So it's worth being deliberate about what earns a spot.  A useful bar for inclusion is: a line belongs if it's one of  1. **Behavior-changing** — a rule that makes the agent do something it otherwise wouldn't. 2. **A non-obvious gotcha** — something that would cost a wasted cycle to rediscover. 3. **An un-guessable name/command** — an exact task name, flag, or path the model can't reliably derive.  And the things that are better left out are the ones that tend to accumulate: content that's easily **discoverable** from the repo itself (`Makefile`, `pixi.toml`, `CMakeLists`, a `grep` of `src/`), content the harness **already surfaces** (Claude Code lists the available skills to the agent every session), **generic advice** the model already follows, **duplicated** info, and anything that can quietly drift **out of date**.  This PR applies that lens. The file goes from ~256 lines to 62. Here's the reasoning behind each change so reviewers can sanity-check the calls.  ### Refreshed a few details that had drifted  The codebase has moved faster than the doc in a couple of places, so these are just catch-ups. They matter a bit more than the rest, because an always-loaded instruction that's out of date can nudge the agent in the wrong direction.  - **\"Adding New Operators\" referenced the older layout** (`src/include/operator/gpu_physical_*`, `src/operator/`, `src/gpu_physical_plan_generator.cpp`) rather than the current Super Sirius one (`src/op/`, `src/planner/sirius_physical_plan_generator.cpp`). Removed — see the \"generic advice\" note below for why it isn't replaced 1:1. - **Fallback section pointed at `src/fallback.cpp`**, which now lives under `src/legacy/`; the current mechanism is the downgrade executor (`src/downgrade/`, `src/creator/`). Updated to point there and to `docs/super-sirius/memory-management.md`. - **`make test` was described as running SQLLogicTests**; it actually builds and runs the C++ Catch2 unit tests (`test → test_release → release`, then `sirius_unittest`), which is also what CI runs. Corrected. - **Logging default** said `SIRIUS_LOG_DIR` defaults to `${CMAKE_BINARY_DIR}/log`; the code default is `log` (relative to CWD). (Then removed — see below.) - **The legacy-path note** listed `src/operator/`, `src/plan/`, `src/gpu_executor.cpp`; legacy now lives under `src/legacy/`. Corrected and kept — the \"the live path is Super Sirius, leave `src/legacy/` alone\" rule is one of the most valuable lines in the file.  ### Removed things the repo itself already answers  These are quicker and more reliable for the model to read straight from the source, where they stay current automatically:  - **\"Key Integration Points\" and \"CMake Notes\"** — CUDA version, C++20, separable compilation, GPU arch list, link libraries — all live in `pixi.toml` / `CMakeLists`. - **\"Code Organization\"** — a prose restatement of the directory layout, obtainable by listing `src/`. - **\"Supported Features\"** — the data-type / operator / join-type inventory. This is both discoverable and the fastest-moving content in the file, so a copy here tends to lag behind. - **Build-outputs list** — the two artifact paths that are actually used still appear inline in the test and usage snippets, so the standalone list was duplicative. - **\"Code Formatting & Linting\"** — collapsed to `pre-commit run -a`; the tool and config-file list lives in `.pre-commit-config.yaml` / `.clang-format`.  ### Removed what the harness already provides  - **The Claude Code skills table.** Claude Code lists the available skills (with descriptions) to the agent at the start of every session, so re-listing them here duplicates that and adds a second place to keep in sync. The one behavioral bit — \"run `/module-context` before implementation work\" — is kept as a one-line directive, since that's a workflow instruction rather than a skills listing.  ### Trimmed advice the model already follows  - **The step-by-step \"Adding New Operators\" recipe.** Once refreshed it came down to \"follow the existing operator pattern,\" which is the default behavior, and the paths overlapped the Architecture section. Pointing the model at the existing operators is more accurate and self-maintaining than a hand-written recipe.  ### Moved niche reference toward the docs  - **Logging env vars** (`SIRIUS_LOG_DIR` / `SIRIUS_LOG_LEVEL`) — handy when you're changing log output, greppable in `src/config.cpp` at that moment. - **`tools/parse_pipeline_log.py`** — useful when debugging wrong query results; discoverable in `tools/` when that comes up.  These two are the most debatable cuts — they're small and occasionally convenient. The reasoning is that per-session context is best spent on what helps *most* sessions, with occasional-use material living in `docs/`. Very happy to keep them if reviewers feel the convenience wins out.  ### What stayed (and why it clears the bar)  - **Default branch is `dev`** — non-obvious, and it determines where branches/PRs go. - **Build/test commands wrapped in `pixi run`** — the `pixi run <cmd>` form (vs. the interactive `pixi shell`) is the key thing for an agent, plus the OOM-parallelism and `make clean` notes. - **The two non-obvious test invocations** — a SQLLogic file via the DuckDB `unittest` runner, and Catch2 tag/test selection on `sirius_unittest` (two different binaries). - **Python API build** — the `build-duckdb-python` task name and the `DUCKDB_SOURCE_PATH` reason. - **Worktree submodule gotcha** — submodules aren't auto-initialized. - **Super Sirius vs. legacy** — live engine in `src/op/` + `src/planner/`; `src/legacy/` is the older path; read `docs/super-sirius/` before changing Super Sirius code. - **Transparent execution** — loading the extension transparently intercepts SQL (`gpu_execution` setting, on by default), with a minimal LOAD + toggle example.  ### Net effect  `CLAUDE.md`: **256 → 62 lines.** Everything retained is something hard for the model to derive on its own; everything removed is something the repo, the harness, or the docs already cover.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") [#874](https://github.com/sirius-db/sirius/pull/874) [)](https://github.com/sirius-db/sirius/commit/24f7461ef785794e2c993e125faa74a94c52800c "docs: trim CLAUDE.md to non-obvious, behavior-changing guidance (#874)  ## Trim `CLAUDE.md` to non-obvious, behavior-changing guidance  `CLAUDE.md` has grown organically as the project has — which is natural, and it means it now covers a lot of ground. This PR is a focused tidy-up. The goal is to make it as useful as possible for Claude Code by keeping it tight and current.  The thing that makes `CLAUDE.md` different from a README: it's **injected into the context of every Claude Code session in this repo**. A human opens a README when they want it; the model reads `CLAUDE.md` on *every* task. That changes the cost/benefit. Each line is a small recurring cost, and — more importantly — the fewer lines there are, the more attention lands on the rules that really matter. So it's worth being deliberate about what earns a spot.  A useful bar for inclusion is: a line belongs if it's one of  1. **Behavior-changing** — a rule that makes the agent do something it otherwise wouldn't. 2. **A non-obvious gotcha** — something that would cost a wasted cycle to rediscover. 3. **An un-guessable name/command** — an exact task name, flag, or path the model can't reliably derive.  And the things that are better left out are the ones that tend to accumulate: content that's easily **discoverable** from the repo itself (`Makefile`, `pixi.toml`, `CMakeLists`, a `grep` of `src/`), content the harness **already surfaces** (Claude Code lists the available skills to the agent every session), **generic advice** the model already follows, **duplicated** info, and anything that can quietly drift **out of date**.  This PR applies that lens. The file goes from ~256 lines to 62. Here's the reasoning behind each change so reviewers can sanity-check the calls.  ### Refreshed a few details that had drifted  The codebase has moved faster than the doc in a couple of places, so these are just catch-ups. They matter a bit more than the rest, because an always-loaded instruction that's out of date can nudge the agent in the wrong direction.  - **\"Adding New Operators\" referenced the older layout** (`src/include/operator/gpu_physical_*`, `src/operator/`, `src/gpu_physical_plan_generator.cpp`) rather than the current Super Sirius one (`src/op/`, `src/planner/sirius_physical_plan_generator.cpp`). Removed — see the \"generic advice\" note below for why it isn't replaced 1:1. - **Fallback section pointed at `src/fallback.cpp`**, which now lives under `src/legacy/`; the current mechanism is the downgrade executor (`src/downgrade/`, `src/creator/`). Updated to point there and to `docs/super-sirius/memory-management.md`. - **`make test` was described as running SQLLogicTests**; it actually builds and runs the C++ Catch2 unit tests (`test → test_release → release`, then `sirius_unittest`), which is also what CI runs. Corrected. - **Logging default** said `SIRIUS_LOG_DIR` defaults to `${CMAKE_BINARY_DIR}/log`; the code default is `log` (relative to CWD). (Then removed — see below.) - **The legacy-path note** listed `src/operator/`, `src/plan/`, `src/gpu_executor.cpp`; legacy now lives under `src/legacy/`. Corrected and kept — the \"the live path is Super Sirius, leave `src/legacy/` alone\" rule is one of the most valuable lines in the file.  ### Removed things the repo itself already answers  These are quicker and more reliable for the model to read straight from the source, where they stay current automatically:  - **\"Key Integration Points\" and \"CMake Notes\"** — CUDA version, C++20, separable compilation, GPU arch list, link libraries — all live in `pixi.toml` / `CMakeLists`. - **\"Code Organization\"** — a prose restatement of the directory layout, obtainable by listing `src/`. - **\"Supported Features\"** — the data-type / operator / join-type inventory. This is both discoverable and the fastest-moving content in the file, so a copy here tends to lag behind. - **Build-outputs list** — the two artifact paths that are actually used still appear inline in the test and usage snippets, so the standalone list was duplicative. - **\"Code Formatting & Linting\"** — collapsed to `pre-commit run -a`; the tool and config-file list lives in `.pre-commit-config.yaml` / `.clang-format`.  ### Removed what the harness already provides  - **The Claude Code skills table.** Claude Code lists the available skills (with descriptions) to the agent at the start of every session, so re-listing them here duplicates that and adds a second place to keep in sync. The one behavioral bit — \"run `/module-context` before implementation work\" — is kept as a one-line directive, since that's a workflow instruction rather than a skills listing.  ### Trimmed advice the model already follows  - **The step-by-step \"Adding New Operators\" recipe.** Once refreshed it came down to \"follow the existing operator pattern,\" which is the default behavior, and the paths overlapped the Architecture section. Pointing the model at the existing operators is more accurate and self-maintaining than a hand-written recipe.  ### Moved niche reference toward the docs  - **Logging env vars** (`SIRIUS_LOG_DIR` / `SIRIUS_LOG_LEVEL`) — handy when you're changing log output, greppable in `src/config.cpp` at that moment. - **`tools/parse_pipeline_log.py`** — useful when debugging wrong query results; discoverable in `tools/` when that comes up.  These two are the most debatable cuts — they're small and occasionally convenient. The reasoning is that per-session context is best spent on what helps *most* sessions, with occasional-use material living in `docs/`. Very happy to keep them if reviewers feel the convenience wins out.  ### What stayed (and why it clears the bar)  - **Default branch is `dev`** — non-obvious, and it determines where branches/PRs go. - **Build/test commands wrapped in `pixi run`** — the `pixi run <cmd>` form (vs. the interactive `pixi shell`) is the key thing for an agent, plus the OOM-parallelism and `make clean` notes. - **The two non-obvious test invocations** — a SQLLogic file via the DuckDB `unittest` runner, and Catch2 tag/test selection on `sirius_unittest` (two different binaries). - **Python API build** — the `build-duckdb-python` task name and the `DUCKDB_SOURCE_PATH` reason. - **Worktree submodule gotcha** — submodules aren't auto-initialized. - **Super Sirius vs. legacy** — live engine in `src/op/` + `src/planner/`; `src/legacy/` is the older path; read `docs/super-sirius/` before changing Super Sirius code. - **Transparent execution** — loading the extension transparently intercepts SQL (`gpu_execution` setting, on by default), with a minimal LOAD + toggle example.  ### Net effect  `CLAUDE.md`: **256 → 62 lines.** Everything retained is something hard for the model to derive on its own; everything removed is something the repo, the harness, or the docs already cover.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  ---------  Co-authored-by: Claude Opus 4.8 (1M context) <noreply@anthropic.com>") | last monthJun 4, 2026 |
| [CMakeLists.txt](https://github.com/sirius-db/sirius/blob/dev/CMakeLists.txt "CMakeLists.txt") | [CMakeLists.txt](https://github.com/sirius-db/sirius/blob/dev/CMakeLists.txt "CMakeLists.txt") | [Added ARRAY data type (fixed-sized lists) (](https://github.com/sirius-db/sirius/commit/a1ab4288feb7681ec704c0ada1089270f80cc472 "Added ARRAY data type (fixed-sized lists) (#901)  ## Add ARRAY (fixed-size list) data type support for native scan path  ### Summary - Maps DuckDB's ARRAY type to cuDF LIST in the type system. - cuCascade handles the H2D transfer and memory management for the LIST column. - list_offset_fixup.cpp/.hpp: a temporary workaround (see [#147](https://github.com/NVIDIA/cuCascade/issues/147)).  ### Changes **Type system** - `src/include/helper/logical_type.hpp`: add the ARRAY logical type. - `src/helper/type_conversions.cpp`: ARRAY to/from DuckDB conversion. - `src/include/cudf/cudf_utils.hpp`: map ARRAY to/from cuDF LIST.  **Scan - Metadata walk** - `src/include/op/scan/duckdb_native_metadata.hpp`: add ARRAY child-segment buckets and is_array flag to column metadata. - `src/op/scan/duckdb_native_metadata.cpp`: walk an ARRAY column's three segment trees into their buckets.  **Scan - Decode** - `src/op/scan/duckdb_native_decoder.{cpp,hpp}`: stage/decode ARRAY columns on the GPU; enable ALP/ALPRD codecs.  **Read-back** - `src/include/op/result/host_table_chunk_reader.hpp`: add child mask accessor / child null count fields. - `src/op/result/host_table_chunk_reader.cpp`: copy element-level child validity back into the DuckDB ARRAY vector.  **LIST offset fixup** - `src/helper/list_offset_fixup.cpp`, `src/include/cudf/list_offset_fixup.hpp`: recast LIST offsets back to INT32 after cuCascade's H2D promotes them to INT64. - `src/include/pipeline/batch_lock_utils.hpp`: apply the fixup after the H2D conversion. - `CMakeLists.txt`: add list_offset_fixup.cpp and tests to the build.  **Tests** - `test/cpp/helper/test_logical_type.cpp`: ARRAY logical type tests. - `test/cpp/helper/test_cudf_utils.cpp`: ARRAY to cuDF LIST mapping tests. - `test/cpp/scan/test_duckdb_native_walker.cpp`: unit tests for the ARRAY metadata walk. - `test/cpp/integration/test_gpu_execution_array.cpp`: end-to-end ARRAY scan tests. - `test/cpp/utils/gpu_execution_fixture.hpp`: shared file-backed native-scan fixture + GPU-vs-CPU comparator") [#901](https://github.com/sirius-db/sirius/pull/901) [)](https://github.com/sirius-db/sirius/commit/a1ab4288feb7681ec704c0ada1089270f80cc472 "Added ARRAY data type (fixed-sized lists) (#901)  ## Add ARRAY (fixed-size list) data type support for native scan path  ### Summary - Maps DuckDB's ARRAY type to cuDF LIST in the type system. - cuCascade handles the H2D transfer and memory management for the LIST column. - list_offset_fixup.cpp/.hpp: a temporary workaround (see [#147](https://github.com/NVIDIA/cuCascade/issues/147)).  ### Changes **Type system** - `src/include/helper/logical_type.hpp`: add the ARRAY logical type. - `src/helper/type_conversions.cpp`: ARRAY to/from DuckDB conversion. - `src/include/cudf/cudf_utils.hpp`: map ARRAY to/from cuDF LIST.  **Scan - Metadata walk** - `src/include/op/scan/duckdb_native_metadata.hpp`: add ARRAY child-segment buckets and is_array flag to column metadata. - `src/op/scan/duckdb_native_metadata.cpp`: walk an ARRAY column's three segment trees into their buckets.  **Scan - Decode** - `src/op/scan/duckdb_native_decoder.{cpp,hpp}`: stage/decode ARRAY columns on the GPU; enable ALP/ALPRD codecs.  **Read-back** - `src/include/op/result/host_table_chunk_reader.hpp`: add child mask accessor / child null count fields. - `src/op/result/host_table_chunk_reader.cpp`: copy element-level child validity back into the DuckDB ARRAY vector.  **LIST offset fixup** - `src/helper/list_offset_fixup.cpp`, `src/include/cudf/list_offset_fixup.hpp`: recast LIST offsets back to INT32 after cuCascade's H2D promotes them to INT64. - `src/include/pipeline/batch_lock_utils.hpp`: apply the fixup after the H2D conversion. - `CMakeLists.txt`: add list_offset_fixup.cpp and tests to the build.  **Tests** - `test/cpp/helper/test_logical_type.cpp`: ARRAY logical type tests. - `test/cpp/helper/test_cudf_utils.cpp`: ARRAY to cuDF LIST mapping tests. - `test/cpp/scan/test_duckdb_native_walker.cpp`: unit tests for the ARRAY metadata walk. - `test/cpp/integration/test_gpu_execution_array.cpp`: end-to-end ARRAY scan tests. - `test/cpp/utils/gpu_execution_fixture.hpp`: shared file-backed native-scan fixture + GPU-vs-CPU comparator") | 3 hours agoJul 2, 2026 |
| [CONTRIBUTING.md](https://github.com/sirius-db/sirius/blob/dev/CONTRIBUTING.md "CONTRIBUTING.md") | [CONTRIBUTING.md](https://github.com/sirius-db/sirius/blob/dev/CONTRIBUTING.md "CONTRIBUTING.md") | [CI: Improve CI GPU Testing reliability and debugging (](https://github.com/sirius-db/sirius/commit/e48de37cfdd73c4d3781f339c7851dd130fbde97 "CI: Improve CI GPU Testing reliability and debugging (#755)  This PR improves CI debugging by uploading the unit test logs from the GPU Test workflow in order for devs to review on failed or timed out unit test runs. The log level is `SIRIUS_LOG_LEVEL=trace` for the unit tests now to help with debugging. Unit tests now also time out after `45 min` so there is time to upload the logs before the runner times out.  In order to assist with debugging, an initial `CONTRIBUTING.md` has been added detailing how to view the unit test as well as the TPC-H logs for debugging.  In addition to this change, the CUDA cache has been updated to use the working directory of the runner, ensuring a clean cache each run.") [#755](https://github.com/sirius-db/sirius/pull/755) [)](https://github.com/sirius-db/sirius/commit/e48de37cfdd73c4d3781f339c7851dd130fbde97 "CI: Improve CI GPU Testing reliability and debugging (#755)  This PR improves CI debugging by uploading the unit test logs from the GPU Test workflow in order for devs to review on failed or timed out unit test runs. The log level is `SIRIUS_LOG_LEVEL=trace` for the unit tests now to help with debugging. Unit tests now also time out after `45 min` so there is time to upload the logs before the runner times out.  In order to assist with debugging, an initial `CONTRIBUTING.md` has been added detailing how to view the unit test as well as the TPC-H logs for debugging.  In addition to this change, the CUDA cache has been updated to use the working directory of the runner, ensuring a clean cache each run.") | 2 months agoMay 5, 2026 |
| [LICENSE](https://github.com/sirius-db/sirius/blob/dev/LICENSE "LICENSE") | [LICENSE](https://github.com/sirius-db/sirius/blob/dev/LICENSE "LICENSE") | [Update LICENSE](https://github.com/sirius-db/sirius/commit/c9c0d81ff08063abb26fcb08506657fe75efb0dd "Update LICENSE") | last yearJun 25, 2025 |
| [Makefile](https://github.com/sirius-db/sirius/blob/dev/Makefile "Makefile") | [Makefile](https://github.com/sirius-db/sirius/blob/dev/Makefile "Makefile") | [Add real-AWS s3-bench test case for future EC2+S3 testing (](https://github.com/sirius-db/sirius/commit/cdfb7d8f9b9cb303401aa99c4fc418a18e290a33 "Add real-AWS s3-bench test case for future EC2+S3 testing (#1049)  ## Summary  This PR restores the manual real-AWS gates that were lost in the REST I/O cutover (#997/#1042), now running against `rest_ioctx`, and adds a connection-scaling benchmark that answers a concrete tuning question: **does raising `max_connections` raise S3 bandwidth on a high-latency path?** (Yes — ~10×; data below.)  ## What's in  - **Live SQL correctness** (`make s3-test-aws-sigv4`): `read_parquet('s3://…')` row-level equality against a local oracle plus aggregates, in **both** signing modes (presigned URL and header SigV4), through the normal `s3://` → `rest_ioctx` routing. Verified green against real AWS us-east-2 (306 assertions) — this exercises real TLS, redirects, clock and STS-token behavior that MinIO cannot. - **AWS bench scenarios** (`SIRIUS_BENCH_BACKEND=aws-s3 make s3-bench`): `aws_https_projected` / `aws_https_full` over SF1 lineitem, each at `max_connections ∈ {1, 32}`, emitting the same perf JSON schema as the MinIO bench with `backend: rest_aws`. `SIRIUS_BENCH_S3_KEY` overrides the object key so an in-region runner can point at SF10. - **Recording plumbing**: per-backend baseline files (`perf-baseline-{minio,aws}.json`, host-keyed, gitignored), a looser AWS WARN threshold (WAN jitter), and per-run history appended to `perf-history-aws.jsonl` for cross-session trend analysis. - **Env guards** with unit tests that run without any AWS access (`[s3][aws][env]`). - Makefile: `s3-bench` selects by `SIRIUS_BENCH_BACKEND` (aws-s3 → `[s3][bench][aws]`, never starts MinIO); the correctness gates exclude `[bench]`.  ## Perf results (real AWS, us-east-2, SF1 lineitem 205.7 MB, median of 2 steady runs)  | scenario | max_connections | wall | footer_fetch | scan | effective throughput | |---|---|---|---|---|---| | projected (9.4 MB) | 1 | 12.9 s | 1.54 s | 10.7 s | 0.73 MB/s | | full (205.7 MB) | 1 | 115.7 s | 1.72 s | 113.2 s | 1.78 MB/s | | projected (9.4 MB) | 32 | 5.3 s | 1.89 s | 2.5 s | 1.92 MB/s | | full (205.7 MB) | 32 | **11.8 s** | 1.93 s | 8.5 s | **17.5 MB/s** |  MinIO loopback reference (same backend/instrumentation, SF10): footer 2.3–8.8 **ms**, full-scan 967 MB/s (HTTP) / 784 MB/s (HTTPS) at mc=32.  ### Analysis  1. **Connection scaling is the bandwidth lever on high-latency paths: mc=1 → 32 = ~10× wall and ~10× effective throughput** (scan-phase-only ratio 12.9–13.6×). A single TCP/TLS stream is bounded by `window / RTT` — on this path one connection is pinned at ~1.8 MB/s regardless of server capacity; 32 parallel streams lift the aggregate to 17.5 MB/s. Scaling is sub-linear (10× for 32×) because the path's aggregate capacity becomes the new ceiling (per-connection efficiency drops 1.78 → ~0.55 MB/s). In effect: `throughput ≈ min(path_capacity, N × window/RTT)`. 2. **Connections do not buy latency**: `footer_fetch` is flat across mc=1/32 in every run (~1.5–1.9 s here vs 2.3–8.8 ms on loopback, ~500–800×) — the footer is one ranged GET, one round-trip, and parallelism cannot shorten a round-trip. Small/projected reads are therefore bind-dominated on WAN (projected@mc=32: 5.3 s wall, only 2.5 s scanning); the lever there is footer/metadata reuse, and the bind cost is per-file, so it multiplies with file count. 3. **On loopback the same knob is not the lever**: RTT ≈ 0 means a single stream can already saturate; bandwidth is bound by TLS/CPU (967 HTTP vs 784 HTTPS = the per-byte TLS cost). `max_connections=32` as the default is right for WAN and harmless locally. 4. **Invariants held in every cell of both columns**: `device_stream_sync_total == 0` (the GPU async-copy path never inline-syncs, latency-regime-independent), payload bytes byte-identical across runs, `retries_total == 0`, `terminal_failures_total == 0`. 5. Same-path comparison vs the pre-cutover async backend (measured 2026-06-22): footer 1.5–1.9 s (was 2.4–4.4 s), steady throughput 17.5 MB/s (was 10.4–11.7) — the REST backend is same-shape but better on both bind and throughput.") [#1049](https://github.com/sirius-db/sirius/pull/1049) [)](https://github.com/sirius-db/sirius/commit/cdfb7d8f9b9cb303401aa99c4fc418a18e290a33 "Add real-AWS s3-bench test case for future EC2+S3 testing (#1049)  ## Summary  This PR restores the manual real-AWS gates that were lost in the REST I/O cutover (#997/#1042), now running against `rest_ioctx`, and adds a connection-scaling benchmark that answers a concrete tuning question: **does raising `max_connections` raise S3 bandwidth on a high-latency path?** (Yes — ~10×; data below.)  ## What's in  - **Live SQL correctness** (`make s3-test-aws-sigv4`): `read_parquet('s3://…')` row-level equality against a local oracle plus aggregates, in **both** signing modes (presigned URL and header SigV4), through the normal `s3://` → `rest_ioctx` routing. Verified green against real AWS us-east-2 (306 assertions) — this exercises real TLS, redirects, clock and STS-token behavior that MinIO cannot. - **AWS bench scenarios** (`SIRIUS_BENCH_BACKEND=aws-s3 make s3-bench`): `aws_https_projected` / `aws_https_full` over SF1 lineitem, each at `max_connections ∈ {1, 32}`, emitting the same perf JSON schema as the MinIO bench with `backend: rest_aws`. `SIRIUS_BENCH_S3_KEY` overrides the object key so an in-region runner can point at SF10. - **Recording plumbing**: per-backend baseline files (`perf-baseline-{minio,aws}.json`, host-keyed, gitignored), a looser AWS WARN threshold (WAN jitter), and per-run history appended to `perf-history-aws.jsonl` for cross-session trend analysis. - **Env guards** with unit tests that run without any AWS access (`[s3][aws][env]`). - Makefile: `s3-bench` selects by `SIRIUS_BENCH_BACKEND` (aws-s3 → `[s3][bench][aws]`, never starts MinIO); the correctness gates exclude `[bench]`.  ## Perf results (real AWS, us-east-2, SF1 lineitem 205.7 MB, median of 2 steady runs)  | scenario | max_connections | wall | footer_fetch | scan | effective throughput | |---|---|---|---|---|---| | projected (9.4 MB) | 1 | 12.9 s | 1.54 s | 10.7 s | 0.73 MB/s | | full (205.7 MB) | 1 | 115.7 s | 1.72 s | 113.2 s | 1.78 MB/s | | projected (9.4 MB) | 32 | 5.3 s | 1.89 s | 2.5 s | 1.92 MB/s | | full (205.7 MB) | 32 | **11.8 s** | 1.93 s | 8.5 s | **17.5 MB/s** |  MinIO loopback reference (same backend/instrumentation, SF10): footer 2.3–8.8 **ms**, full-scan 967 MB/s (HTTP) / 784 MB/s (HTTPS) at mc=32.  ### Analysis  1. **Connection scaling is the bandwidth lever on high-latency paths: mc=1 → 32 = ~10× wall and ~10× effective throughput** (scan-phase-only ratio 12.9–13.6×). A single TCP/TLS stream is bounded by `window / RTT` — on this path one connection is pinned at ~1.8 MB/s regardless of server capacity; 32 parallel streams lift the aggregate to 17.5 MB/s. Scaling is sub-linear (10× for 32×) because the path's aggregate capacity becomes the new ceiling (per-connection efficiency drops 1.78 → ~0.55 MB/s). In effect: `throughput ≈ min(path_capacity, N × window/RTT)`. 2. **Connections do not buy latency**: `footer_fetch` is flat across mc=1/32 in every run (~1.5–1.9 s here vs 2.3–8.8 ms on loopback, ~500–800×) — the footer is one ranged GET, one round-trip, and parallelism cannot shorten a round-trip. Small/projected reads are therefore bind-dominated on WAN (projected@mc=32: 5.3 s wall, only 2.5 s scanning); the lever there is footer/metadata reuse, and the bind cost is per-file, so it multiplies with file count. 3. **On loopback the same knob is not the lever**: RTT ≈ 0 means a single stream can already saturate; bandwidth is bound by TLS/CPU (967 HTTP vs 784 HTTPS = the per-byte TLS cost). `max_connections=32` as the default is right for WAN and harmless locally. 4. **Invariants held in every cell of both columns**: `device_stream_sync_total == 0` (the GPU async-copy path never inline-syncs, latency-regime-independent), payload bytes byte-identical across runs, `retries_total == 0`, `terminal_failures_total == 0`. 5. Same-path comparison vs the pre-cutover async backend (measured 2026-06-22): footer 1.5–1.9 s (was 2.4–4.4 s), steady throughput 17.5 MB/s (was 10.4–11.7) — the REST backend is same-shape but better on both bind and throughput.") | 19 hours agoJul 2, 2026 |
| [extension\_config.cmake](https://github.com/sirius-db/sirius/blob/dev/extension_config.cmake "extension_config.cmake") | [extension\_config.cmake](https://github.com/sirius-db/sirius/blob/dev/extension_config.cmake "extension_config.cmake") | [Simplify CMake build: target-scoped properties, cleanup (](https://github.com/sirius-db/sirius/commit/63062460947b1ca3e42195ef8c9f7dccbbe3aac5 "Simplify CMake build: target-scoped properties, cleanup (#356)  * Simplify CMake build: GLOB_RECURSE, target-scoped properties, cleanup  - Replace 28 subdirectory CMakeLists.txt with file(GLOB_RECURSE CONFIGURE_DEPENDS) - Replace global include_directories/add_compile_definitions with target-scoped equivalents - Replace find_library for numa with pkg-config (PkgConfig::NUMA) - Move CMAKE_EXPORT_COMPILE_COMMANDS to CMake presets - Inline cuCascade setup directly in CMakeLists.txt, remove third_party/ - Remove unused: ENABLE_CUDF define, -rdc=true flag, -std=c++20 flag,   SKIP_SUBSTRAIT_C_TESTS, CUDA --expt-* flags (default since CUDA 12.2) - Remove bundled DuckDB extensions (json, tpcds, tpch, parquet, icu) from   extension_config.cmake - Remove orphaned sources: memmove.cu, downgrade_task_creator.{cpp,hpp} - Remove commented-out dead code (substrait refs, old link lines) - Consolidate set_target_properties and target_link_libraries per target - Exclude stale disabled tests (test_modified_pipeline, test_task_creator)  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  Format  Restore parquet extension  * Fix segfault: restore previous cudf device resource on destruction  sirius_memory_reservation_manager's destructor called cudf::reset_current_device_resource_ref() which left a dangling resource_ref after the underlying memory space was destroyed. This caused a NULL function pointer call in RMM when subsequent code (e.g. cudf's parquet reader) allocated device memory through the global cudf device resource.  Save the previous resource in the constructor and restore it in the destructor instead of resetting to a default that references the already-destroyed allocator.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Update pixi.lock  * Deduplicate CMake config with foreach, generator expressions, cleanup  - Replace repeated sirius_extension/sirius_loadable_extension blocks with   a single foreach loop for shared properties, includes, definitions, and   link libraries - Use $<BUILD_INTERFACE:...> for include directories and path-based   compile definitions, following cucascade's patterns - Remove redundant per-target CUDA_SEPARABLE_COMPILATION (already set   globally via CMAKE_CUDA_SEPARABLE_COMPILATION) - Consolidate install rules into one call with comment explaining the   DuckDB export set requirement; remove unused stream_check install - Remove setup_sirius.sh (superseded by pixi) - Inline TEST_NAME variable; add FATAL_ERROR to cmake_minimum_required  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Use mold linker for faster link times  Add mold to pixi dependencies and set CMAKE_LINKER_TYPE=MOLD in the base CMake preset so all build configurations use it by default.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Set CUDA_RESOLVE_DEVICE_SYMBOLS globally, remove per-target property  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove explicit LINKER_LANGUAGE CUDA, CMake infers it from .cu sources  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Use CONFIG mode for libconfig++ and absl find_package calls  Ensures deterministic package resolution from the pixi environment instead of falling back to system Find-modules.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Normalize include paths to use only src/include  Remove src/include/operator and src/include/cudf from the CMake include directories. Instead, qualify every include of a header living under those subdirectories with its operator/ or cudf/ prefix so that a single -Isrc/include entry suffices.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove 430 unused #include directives across 119 files  Ran clang-include-cleaner with --edit --disable-insert (removals only, no insertions) on all .cpp files under src/ and test/. Restored 8 files where the tool produced false positives due to transitive dependencies, implicit conversions, or incomplete type requirements.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Add precompiled headers to reduce build times  Precompile frequently-included headers for extension targets (STL, spdlog/logging, DuckDB planner) and the Catch2 header for tests. The loadable extension reuses the static extension's PCH to avoid compiling it twice. CUDA sources are excluded since nvcc doesn't support CMake's PCH mechanism.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Format  * Fix debug build  * Fix mixed right semi/anti join inequality operator not flipped on side swap  When swap_sides=true (used for RIGHT_SEMI and RIGHT_ANTI mixed joins), the table references for left/right expressions were correctly swapped, but the inequality comparison operators were not flipped. This meant e.g. \"L.a > R.b\" became \"R.a > L.b\" instead of the correct \"R.a < L.b\", producing wrong join results.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Ensure CMake presets symlink exists before build targets  The Makefile creates a duckdb/CMakePresets.json symlink pointing to cmake/CMakePresets.json as a prerequisite of all build targets. This ensures cmake --preset works even if the pixi activation script hasn't run (e.g. when invoking make directly outside pixi).  Use CMakePresets.json (not CMakeUserPresets.json) because CMake 4.x requires a CMakePresets.json to exist alongside CMakeUserPresets.json.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Revert translator operator flip, update DuckDB to 1.4.4  The mixed right semi/anti join test failure is caused by a DuckDB bug (duckdb/duckdb#20410) fixed in v1.4.4 (commit fc87d53e32), not by the expression translator. Revert the incorrect operator flip and update the DuckDB submodule from d1dc88f950 (v1.4.3) to 6ddac802 (v1.4.4) to match the dev branch.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Fix include path for cudf_utils.hpp after dev merge  Code from dev used the old-style #include <cudf_utils.hpp>, but cmake-cleanup normalized includes to use the cudf/ subdirectory.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Update submodule to match dev  * Remove unused #include directives flagged by clang-include-cleaner  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Move language standard settings from global scope to target level  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Replace GLOB_RECURSE with explicit file lists in CMakeLists.txt  Avoid file(GLOB_RECURSE CONFIGURE_DEPENDS) for source files due to portability, build performance, and accidental inclusion risks. List all source files explicitly for EXTENSION_SOURCES, CUDA_SOURCES, and TEST_SOURCES.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") [#356](https://github.com/sirius-db/sirius/pull/356) [)](https://github.com/sirius-db/sirius/commit/63062460947b1ca3e42195ef8c9f7dccbbe3aac5 "Simplify CMake build: target-scoped properties, cleanup (#356)  * Simplify CMake build: GLOB_RECURSE, target-scoped properties, cleanup  - Replace 28 subdirectory CMakeLists.txt with file(GLOB_RECURSE CONFIGURE_DEPENDS) - Replace global include_directories/add_compile_definitions with target-scoped equivalents - Replace find_library for numa with pkg-config (PkgConfig::NUMA) - Move CMAKE_EXPORT_COMPILE_COMMANDS to CMake presets - Inline cuCascade setup directly in CMakeLists.txt, remove third_party/ - Remove unused: ENABLE_CUDF define, -rdc=true flag, -std=c++20 flag,   SKIP_SUBSTRAIT_C_TESTS, CUDA --expt-* flags (default since CUDA 12.2) - Remove bundled DuckDB extensions (json, tpcds, tpch, parquet, icu) from   extension_config.cmake - Remove orphaned sources: memmove.cu, downgrade_task_creator.{cpp,hpp} - Remove commented-out dead code (substrait refs, old link lines) - Consolidate set_target_properties and target_link_libraries per target - Exclude stale disabled tests (test_modified_pipeline, test_task_creator)  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  Format  Restore parquet extension  * Fix segfault: restore previous cudf device resource on destruction  sirius_memory_reservation_manager's destructor called cudf::reset_current_device_resource_ref() which left a dangling resource_ref after the underlying memory space was destroyed. This caused a NULL function pointer call in RMM when subsequent code (e.g. cudf's parquet reader) allocated device memory through the global cudf device resource.  Save the previous resource in the constructor and restore it in the destructor instead of resetting to a default that references the already-destroyed allocator.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Update pixi.lock  * Deduplicate CMake config with foreach, generator expressions, cleanup  - Replace repeated sirius_extension/sirius_loadable_extension blocks with   a single foreach loop for shared properties, includes, definitions, and   link libraries - Use $<BUILD_INTERFACE:...> for include directories and path-based   compile definitions, following cucascade's patterns - Remove redundant per-target CUDA_SEPARABLE_COMPILATION (already set   globally via CMAKE_CUDA_SEPARABLE_COMPILATION) - Consolidate install rules into one call with comment explaining the   DuckDB export set requirement; remove unused stream_check install - Remove setup_sirius.sh (superseded by pixi) - Inline TEST_NAME variable; add FATAL_ERROR to cmake_minimum_required  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Use mold linker for faster link times  Add mold to pixi dependencies and set CMAKE_LINKER_TYPE=MOLD in the base CMake preset so all build configurations use it by default.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Set CUDA_RESOLVE_DEVICE_SYMBOLS globally, remove per-target property  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove explicit LINKER_LANGUAGE CUDA, CMake infers it from .cu sources  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Use CONFIG mode for libconfig++ and absl find_package calls  Ensures deterministic package resolution from the pixi environment instead of falling back to system Find-modules.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Normalize include paths to use only src/include  Remove src/include/operator and src/include/cudf from the CMake include directories. Instead, qualify every include of a header living under those subdirectories with its operator/ or cudf/ prefix so that a single -Isrc/include entry suffices.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Remove 430 unused #include directives across 119 files  Ran clang-include-cleaner with --edit --disable-insert (removals only, no insertions) on all .cpp files under src/ and test/. Restored 8 files where the tool produced false positives due to transitive dependencies, implicit conversions, or incomplete type requirements.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Add precompiled headers to reduce build times  Precompile frequently-included headers for extension targets (STL, spdlog/logging, DuckDB planner) and the Catch2 header for tests. The loadable extension reuses the static extension's PCH to avoid compiling it twice. CUDA sources are excluded since nvcc doesn't support CMake's PCH mechanism.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Format  * Fix debug build  * Fix mixed right semi/anti join inequality operator not flipped on side swap  When swap_sides=true (used for RIGHT_SEMI and RIGHT_ANTI mixed joins), the table references for left/right expressions were correctly swapped, but the inequality comparison operators were not flipped. This meant e.g. \"L.a > R.b\" became \"R.a > L.b\" instead of the correct \"R.a < L.b\", producing wrong join results.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Ensure CMake presets symlink exists before build targets  The Makefile creates a duckdb/CMakePresets.json symlink pointing to cmake/CMakePresets.json as a prerequisite of all build targets. This ensures cmake --preset works even if the pixi activation script hasn't run (e.g. when invoking make directly outside pixi).  Use CMakePresets.json (not CMakeUserPresets.json) because CMake 4.x requires a CMakePresets.json to exist alongside CMakeUserPresets.json.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Revert translator operator flip, update DuckDB to 1.4.4  The mixed right semi/anti join test failure is caused by a DuckDB bug (duckdb/duckdb#20410) fixed in v1.4.4 (commit fc87d53e32), not by the expression translator. Revert the incorrect operator flip and update the DuckDB submodule from d1dc88f950 (v1.4.3) to 6ddac802 (v1.4.4) to match the dev branch.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Fix include path for cudf_utils.hpp after dev merge  Code from dev used the old-style #include <cudf_utils.hpp>, but cmake-cleanup normalized includes to use the cudf/ subdirectory.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Format  * Update submodule to match dev  * Remove unused #include directives flagged by clang-include-cleaner  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Move language standard settings from global scope to target level  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  * Replace GLOB_RECURSE with explicit file lists in CMakeLists.txt  Avoid file(GLOB_RECURSE CONFIGURE_DEPENDS) for source files due to portability, build performance, and accidental inclusion risks. List all source files explicitly for EXTENSION_SOURCES, CUDA_SOURCES, and TEST_SOURCES.  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>  ---------  Co-authored-by: Claude Opus 4.6 <noreply@anthropic.com>") | 4 months agoMar 5, 2026 |
| [pixi.lock](https://github.com/sirius-db/sirius/blob/dev/pixi.lock "pixi.lock") | [pixi.lock](https://github.com/sirius-db/sirius/blob/dev/pixi.lock "pixi.lock") | [build(pixi): bump to 0.71 and adopt rich platforms (](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") [#1005](https://github.com/sirius-db/sirius/pull/1005) [)](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | 3 days agoJun 30, 2026 |
| [pixi.toml](https://github.com/sirius-db/sirius/blob/dev/pixi.toml "pixi.toml") | [pixi.toml](https://github.com/sirius-db/sirius/blob/dev/pixi.toml "pixi.toml") | [build(pixi): bump to 0.71 and adopt rich platforms (](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") [#1005](https://github.com/sirius-db/sirius/pull/1005) [)](https://github.com/sirius-db/sirius/commit/9be21579553e5f043c2f50a1ab5821f1ce1f0de1 "build(pixi): bump to 0.71 and adopt rich platforms (#1005)  Bumps pixi to **0.71** and migrates to its [rich-platform CUDA requirements](https://github.com/prefix-dev/pixi/releases/tag/v0.71.0).  ## Why  pixi 0.71 deprecated the per-feature `[system-requirements]` table and turned the CUDA requirement into a *platform-match gate* — so a CUDA environment no longer matches a driverless host. Left unchanged, `pixi list`/`install` fails on every CPU runner with `no platform supported by environment … matches the current system`.  ## What changed  - **Pixi 0.71** across the board: all workflow `pixi-version` inputs (`v0.70.2` → `v0.71.1`), the `nightly-runner` dependency (`0.70.*` → `0.71.*`), and `requires-pixi` (`>=0.68` → `>=0.71`). `setup-pixi` is already on its latest release (`v0.9.6`). - **Rich/named platforms.** Each conda subdir is declared once per supported CUDA major as a named platform pinning `__cuda`; the `cuda12`/`cuda13` features bind to their matching pair (replacing the deprecated `system-requirements`). `CUDAARCHS` (per feature) still carries the codegen arch list. - **GPU-less builds.** The CPU build/lint runners set `CONDA_OVERRIDE_CUDA` so the CUDA environment resolves without a driver; GPU test runners keep detecting the real driver (`__cuda` matches minimum-style, so a current driver satisfies either CUDA major). - `duckdb-python` and `nightly-runner` don't depend on the CUDA major, so they're pinned to the cuda13 pair — otherwise each would carry two `linux-*` platforms differing only by `__cuda`. - **StarRocks experiment manifest** (`experimental/starrocks/pixi.toml`) gets the same migration (its `engine` feature pinned the deprecated `system-requirements`, which pixi 0.71 synthesized into workspace-wide CUDA platforms and broke the CUDA-agnostic `cn` env). - **Nightly env generator** now strips the `nightly-runner` feature blocks in full (range delete) so their bodies don't leak into the generated manifest. - Regenerated both `pixi.lock` files.  > [!NOTE] > Approach mirrors NVIDIA/cuCascade#153. `__cuda_arch` was explored but dropped: pinning it on a platform breaks pixi 0.71.1's current-system match unconditionally (no host value matches, GPU or not), and `system-requirements` can't carry it either.  🤖 Generated with [Claude Code](https://claude.com/claude-code)  Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>") | 3 days agoJun 30, 2026 |
| [setup\_test\_datasets.sh](https://github.com/sirius-db/sirius/blob/dev/setup_test_datasets.sh "setup_test_datasets.sh") | [setup\_test\_datasets.sh](https://github.com/sirius-db/sirius/blob/dev/setup_test_datasets.sh "setup_test_datasets.sh") | [Update DuckDB to v1.5.1 (](https://github.com/sirius-db/sirius/commit/e354d846c0cde8f41b87b4e7f87a8d607131ac59 "Update DuckDB to v1.5.1 (#442)  ## Summary - Update DuckDB submodule from v1.4.4 to v1.5.1 (\"Variegata\") - Update extension-ci-tools and substrait submodules to v1.5.1 - Fix all API breaking changes and GPU execution regressions  ## Changes  ### DuckDB v1.5.1 submodule update - **duckdb**: `v1.4.4` → `v1.5.1` (`7dbb2e6`) - **extension-ci-tools**: `32eb753` → `ef15a2a` (v1.5.1) - **substrait**: `501d558` → `ac24645` (duckdb-1.5.1)  ### API migrations | Old API | New API | Files | |---------|---------|-------| | `DBConfig::GetSetting<T>()` | `Settings::Get<T>()` | 8 files | | `config.extension_callbacks.push_back(make_uniq<>())` | `config.GetCallbackManager().Register(make_shared_ptr<>())` | sirius_extension.cpp | | `QueryResult::Fetch()`/`FetchRaw()` override | `FetchInternal()` override | gpu_query_result.hpp/cpp | | `MetricsType::` | `MetricType::` | gpu_physical_plan_generator.cpp, sirius_physical_plan_generator.cpp | | `client_context.config.errors_as_json` | `Settings::Get<ErrorsAsJSONSetting>(client_context)` | gpu_context.cpp, sirius_interface.cpp |  ### GPU execution fixes - **Table name qualification** (`gpu_physical_table_scan.cpp`): DuckDB v1.5.0's `TableScanToString` now returns fully qualified names (`memory.main.hits`). Extract just the table name for GPU buffer manager lookups. - **Streaming limit** (`gpu_plan_limit.cpp`): v1.5.0 may produce `LogicalLimit` nodes where v1.4.4 used `LogicalTopN`. Use non-parallel `GPUPhysicalStreamingLimit` instead of throwing `NotImplementedException`. - **STATISTICS_PROPAGATION** (`sirius_extension.cpp`): Disable this optimizer for both `gpu_processing` and `gpu_execution` paths. v1.5.0 folds ungrouped MIN/MAX into `EXPRESSION_GET + DUMMY_SCAN` which the GPU pipeline cannot schedule. - **async_result** (`duckdb_scan_task.cpp`, `gpu_physical_table_scan.cpp`): Initialize `TableFunctionInput.async_result` to `IMPLICIT` before calling scan functions (required by v1.5.0). - **RESULT_COLLECTOR port check** (`sirius_engine.cpp`): Skip port check for `RESULT_COLLECTOR` in pipeline debug logging. - **EXPRESSION_GET types** (`sirius_plan_expression_get.cpp`): Use `ExpressionExecutor` to evaluate all expression types, replacing the BIGINT-only constant handler. - **Extension lock** (`extension_lock.cpp`): Allow same-process re-acquisition by checking PID, fixing flock self-deadlock when multiple DuckDB instances run in one test binary.  ### ClickBench test data migration - Switch from custom TSV (`test_hits.tsv`) to official ClickBench Parquet partition (`hits_0.parquet`, 1M rows) - Data loading follows official DuckDB approach: `read_parquet` with `binary_as_string=True` + `SELECT * REPLACE` for temporal columns - Inline expected results in test file (DuckDB v1.5.0 changed `read_csv` empty-field handling, breaking the old `<FILE>:` CSV approach) - Add deterministic tiebreakers to 16 ORDER BY ... LIMIT queries - Add `aktion`, `foto` to codespell ignore list (real ClickBench URL data) - Exclude clickbench test from trailing-whitespace hook (binary data with trailing whitespace)  ### Build system - `CMAKE_CXX_SCAN_FOR_MODULES=OFF` for GCC preset (prevents duplicate symbols with mold) - `duckdb_static` + `link_extension_libraries` + `--allow-multiple-definition` for `sirius_unittest`  ### Merged from dev - **`ENABLE_LEGACY_SIRIUS` CMake option** (#509): Conditionally compile legacy code path - **Remove `sirius_pipeline_hashmap`** (#504): Cleanup unused code - **Super Sirius documentation** (#505): Comprehensive docs for the new execution engine - **Merge queue CI triggers** (#508): Add `merge_group` trigger to check and test workflows - **Downgrade executor fix** (#506): Fix `start()` properly resuming after a `stop()`  ## Test plan - [x] Full clean build compiles - [x] C++ unit tests pass (732 tests) - [x] SQL logic tests pass (clickbench + tpch, 432K+ assertions) - [x] 0 `Error in GPUExecuteQuery` fallbacks - [x] pre-commit checks pass  🤖 Created by Claude  ---------  Co-authored-by: Claude Opus 4.6 (1M context) <noreply@anthropic.com>") [#442](https://github.com/sirius-db/sirius/pull/442) [)](https://github.com/sirius-db/sirius/commit/e354d846c0cde8f41b87b4e7f87a8d607131ac59 "Update DuckDB to v1.5.1 (#442)  ## Summary - Update DuckDB submodule from v1.4.4 to v1.5.1 (\"Variegata\") - Update extension-ci-tools and substrait submodules to v1.5.1 - Fix all API breaking changes and GPU execution regressions  ## Changes  ### DuckDB v1.5.1 submodule update - **duckdb**: `v1.4.4` → `v1.5.1` (`7dbb2e6`) - **extension-ci-tools**: `32eb753` → `ef15a2a` (v1.5.1) - **substrait**: `501d558` → `ac24645` (duckdb-1.5.1)  ### API migrations | Old API | New API | Files | |---------|---------|-------| | `DBConfig::GetSetting<T>()` | `Settings::Get<T>()` | 8 files | | `config.extension_callbacks.push_back(make_uniq<>())` | `config.GetCallbackManager().Register(make_shared_ptr<>())` | sirius_extension.cpp | | `QueryResult::Fetch()`/`FetchRaw()` override | `FetchInternal()` override | gpu_query_result.hpp/cpp | | `MetricsType::` | `MetricType::` | gpu_physical_plan_generator.cpp, sirius_physical_plan_generator.cpp | | `client_context.config.errors_as_json` | `Settings::Get<ErrorsAsJSONSetting>(client_context)` | gpu_context.cpp, sirius_interface.cpp |  ### GPU execution fixes - **Table name qualification** (`gpu_physical_table_scan.cpp`): DuckDB v1.5.0's `TableScanToString` now returns fully qualified names (`memory.main.hits`). Extract just the table name for GPU buffer manager lookups. - **Streaming limit** (`gpu_plan_limit.cpp`): v1.5.0 may produce `LogicalLimit` nodes where v1.4.4 used `LogicalTopN`. Use non-parallel `GPUPhysicalStreamingLimit` instead of throwing `NotImplementedException`. - **STATISTICS_PROPAGATION** (`sirius_extension.cpp`): Disable this optimizer for both `gpu_processing` and `gpu_execution` paths. v1.5.0 folds ungrouped MIN/MAX into `EXPRESSION_GET + DUMMY_SCAN` which the GPU pipeline cannot schedule. - **async_result** (`duckdb_scan_task.cpp`, `gpu_physical_table_scan.cpp`): Initialize `TableFunctionInput.async_result` to `IMPLICIT` before calling scan functions (required by v1.5.0). - **RESULT_COLLECTOR port check** (`sirius_engine.cpp`): Skip port check for `RESULT_COLLECTOR` in pipeline debug logging. - **EXPRESSION_GET types** (`sirius_plan_expression_get.cpp`): Use `ExpressionExecutor` to evaluate all expression types, replacing the BIGINT-only constant handler. - **Extension lock** (`extension_lock.cpp`): Allow same-process re-acquisition by checking PID, fixing flock self-deadlock when multiple DuckDB instances run in one test binary.  ### ClickBench test data migration - Switch from custom TSV (`test_hits.tsv`) to official ClickBench Parquet partition (`hits_0.parquet`, 1M rows) - Data loading follows official DuckDB approach: `read_parquet` with `binary_as_string=True` + `SELECT * REPLACE` for temporal columns - Inline expected results in test file (DuckDB v1.5.0 changed `read_csv` empty-field handling, breaking the old `<FILE>:` CSV approach) - Add deterministic tiebreakers to 16 ORDER BY ... LIMIT queries - Add `aktion`, `foto` to codespell ignore list (real ClickBench URL data) - Exclude clickbench test from trailing-whitespace hook (binary data with trailing whitespace)  ### Build system - `CMAKE_CXX_SCAN_FOR_MODULES=OFF` for GCC preset (prevents duplicate symbols with mold) - `duckdb_static` + `link_extension_libraries` + `--allow-multiple-definition` for `sirius_unittest`  ### Merged from dev - **`ENABLE_LEGACY_SIRIUS` CMake option** (#509): Conditionally compile legacy code path - **Remove `sirius_pipeline_hashmap`** (#504): Cleanup unused code - **Super Sirius documentation** (#505): Comprehensive docs for the new execution engine - **Merge queue CI triggers** (#508): Add `merge_group` trigger to check and test workflows - **Downgrade executor fix** (#506): Fix `start()` properly resuming after a `stop()`  ## Test plan - [x] Full clean build compiles - [x] C++ unit tests pass (732 tests) - [x] SQL logic tests pass (clickbench + tpch, 432K+ assertions) - [x] 0 `Error in GPUExecuteQuery` fallbacks - [x] pre-commit checks pass  🤖 Created by Claude  ---------  Co-authored-by: Claude Opus 4.6 (1M context) <noreply@anthropic.com>") | 4 months agoMar 31, 2026 |
| [tsan.supp](https://github.com/sirius-db/sirius/blob/dev/tsan.supp "tsan.supp") | [tsan.supp](https://github.com/sirius-db/sirius/blob/dev/tsan.supp "tsan.supp") | [ASAN / TSAN builds and core dump facilities (](https://github.com/sirius-db/sirius/commit/cf45a8031a1c894ed8630a871370b3e90e75772d "ASAN / TSAN builds and core dump facilities (#864)  This PR adds a ASAN and TSAN build profile, as well as instructions in a new debugging.md file for how to use them. It also allows for disabling the segfault handler so that its easier to capture core dumps and instructions on how to do so.") [#864](https://github.com/sirius-db/sirius/pull/864) [)](https://github.com/sirius-db/sirius/commit/cf45a8031a1c894ed8630a871370b3e90e75772d "ASAN / TSAN builds and core dump facilities (#864)  This PR adds a ASAN and TSAN build profile, as well as instructions in a new debugging.md file for how to use them. It also allows for disabling the segfault handler so that its easier to capture core dumps and instructions on how to do so.") | last monthJun 4, 2026 |
| [vcpkg.json](https://github.com/sirius-db/sirius/blob/dev/vcpkg.json "vcpkg.json") | [vcpkg.json](https://github.com/sirius-db/sirius/blob/dev/vcpkg.json "vcpkg.json") | [s3datasource backend and scan\_manager integration (replace PR759) (](https://github.com/sirius-db/sirius/commit/52fcf4011236c5e8dcb8c5d9e154527bd9222bf6 "s3datasource backend  and scan_manager integration (replace PR759) (#784)  # Summary This PR adds an S3 datasource backend to Sirius and wires it into `sirius_scan_manager`. It **supersedes PR #759** — the same work, rebased directly onto `dev`, with the PR #759 review feedback addressed.  **Scope — please read before reviewing.** This PR delivers the S3 datasource at the **runtime / C++ level only**:  - ✅ `sirius_scan_manager` routes `s3://` paths to the S3 backend and reads parquet from S3, with prefetch caching and HTTP retry — verified end-to-end against MinIO. - ⚠️ **End-to-end SQL is not connected yet.** `gpu_execution('SELECT … read_parquet(\"s3://…\")')` does not yet route to the S3 backend. The SQL surface needs a bind-time parquet-footer fetch, and the design of that footer handling still needs alignment with @aminaramoon — that is a **follow-up PR**. - ⚠️ **The S3 integration tests are not yet in the default CI unit-test run.** They run as a dedicated, MinIO-gated suite (`make s3-up` + the `[s3]` tag), invoked explicitly. Folding them into the main CI suite is deferred to the follow-up PR.  So: this PR = \"the S3 runtime, C++-verified and ready\"; the follow-up PR = \"SQL reaches it end-to-end, plus AWS coverage and CI integration\".  ## S3 backend resource model — addresses the PR #759 review ### Threads - **No per-request thread creation in production.** Async reads dispatch onto caller-injected thread pool.`sirius_scan_manager` owns that pool (default 8 threads) - **Bounded concurrency.** ≤ 8 worker threads; the libcurl easy-handle pool is capped (default 16 handles) and grown on demand. In-flight HTTP requests are thread-bounded. - **The detached-thread path is test-only.** With no injected pool (standalone / unit-test scenarios), async reads fall back to a per-request detached `std::thread`. Production always injects the pool, so the engine never hits this path.  ### Memory - **The S3 backend holds almost no resident memory of its own.** Host range reads stream libcurl output directly into the caller-provided host buffer; the S3 backend keeps no resident object buffer. - **Device reads use bounded host staging.** S3 has no device-native path, so a device read bounces through a host staging buffer before the H2D copy. That buffer is a single block borrowed from the scan manager's host memory resource (`fixed_size_host_memory_resource`) and reused chunk-by-chunk, so a device read's transient host memory is capped at one FSMR block regardless of the read size. (A standalone build with no injected FSMR keeps a plain `std::vector` fallback — test-only; production always injects the FSMR.) - **The curl handle pool** is a small fixed overhead; each handle keeps libcurl's connection cache so TCP / TLS connections are reused across requests.  ### Retry - **Bounded attempts** — 3 total attempts (2 retries) per request. - **Retriable conditions** — HTTP 408 / 429 / 5xx and transient libcurl codes (connect / timeout class). Everything else — other 4xx, signature / not-found / auth failures — fails fast with no retry. - **Backoff** — exponential with jitter; the server's `Retry-After` header (429 / 503) is honored when present, capped at 30 s. - **Worst-case stuck time per request is bounded** — each attempt is capped by a curl   timeout (default 60 s); total ≈ timeout × attempts + backoff.  ## Non-S3 code changes  Most of the diff is the new `src/io/s3/` module and its tests. The changes to existing engine code are **additive and behavior-preserving for non-S3 paths**: `sirius_scan_manager` now holds a vector of backends and dispatches per path, and `SiriusContext` composes the S3 config from the YAML object-store settings — the local-file path is unchanged. The one behavior change touching a non-S3 path makes a scan_manager-routed local parquet scan fall back to cudf (instead of throwing) when no Sirius backend is configured, restoring the documented behavior under `dev`'s current default.  ## Testing  - **Local MinIO only, so far.** S3 integration tests run against a pinned MinIO container: `make s3-up` → `sirius_unittest \"[s3]\"` → `make s3-down` — 76 test cases, 0 failures. - An optional perf benchmark (`make s3-bench`) is included; it is hidden from the default est run and reachable only via that target.  ## TODO / follow-ups  - **Follow-up PR — SQL surface.** Make `gpu_execution('… read_parquet(\"s3://…\")')` route to the S3 backend end-to-end; - **AWS testing.** S3 integration is currently verified only against local MinIO. Once the   SQL surface lands and the path is end-to-end, add real-AWS testing.   ## Closes  Closes PR #759.  ---------  Co-authored-by: Yu <“ranyuan_jin@outlook.com“> Co-authored-by: Claude Opus 4.7 <noreply@anthropic.com>") [#784](https://github.com/sirius-db/sirius/pull/784) [)](https://github.com/sirius-db/sirius/commit/52fcf4011236c5e8dcb8c5d9e154527bd9222bf6 "s3datasource backend  and scan_manager integration (replace PR759) (#784)  # Summary This PR adds an S3 datasource backend to Sirius and wires it into `sirius_scan_manager`. It **supersedes PR #759** — the same work, rebased directly onto `dev`, with the PR #759 review feedback addressed.  **Scope — please read before reviewing.** This PR delivers the S3 datasource at the **runtime / C++ level only**:  - ✅ `sirius_scan_manager` routes `s3://` paths to the S3 backend and reads parquet from S3, with prefetch caching and HTTP retry — verified end-to-end against MinIO. - ⚠️ **End-to-end SQL is not connected yet.** `gpu_execution('SELECT … read_parquet(\"s3://…\")')` does not yet route to the S3 backend. The SQL surface needs a bind-time parquet-footer fetch, and the design of that footer handling still needs alignment with @aminaramoon — that is a **follow-up PR**. - ⚠️ **The S3 integration tests are not yet in the default CI unit-test run.** They run as a dedicated, MinIO-gated suite (`make s3-up` + the `[s3]` tag), invoked explicitly. Folding them into the main CI suite is deferred to the follow-up PR.  So: this PR = \"the S3 runtime, C++-verified and ready\"; the follow-up PR = \"SQL reaches it end-to-end, plus AWS coverage and CI integration\".  ## S3 backend resource model — addresses the PR #759 review ### Threads - **No per-request thread creation in production.** Async reads dispatch onto caller-injected thread pool.`sirius_scan_manager` owns that pool (default 8 threads) - **Bounded concurrency.** ≤ 8 worker threads; the libcurl easy-handle pool is capped (default 16 handles) and grown on demand. In-flight HTTP requests are thread-bounded. - **The detached-thread path is test-only.** With no injected pool (standalone / unit-test scenarios), async reads fall back to a per-request detached `std::thread`. Production always injects the pool, so the engine never hits this path.  ### Memory - **The S3 backend holds almost no resident memory of its own.** Host range reads stream libcurl output directly into the caller-provided host buffer; the S3 backend keeps no resident object buffer. - **Device reads use bounded host staging.** S3 has no device-native path, so a device read bounces through a host staging buffer before the H2D copy. That buffer is a single block borrowed from the scan manager's host memory resource (`fixed_size_host_memory_resource`) and reused chunk-by-chunk, so a device read's transient host memory is capped at one FSMR block regardless of the read size. (A standalone build with no injected FSMR keeps a plain `std::vector` fallback — test-only; production always injects the FSMR.) - **The curl handle pool** is a small fixed overhead; each handle keeps libcurl's connection cache so TCP / TLS connections are reused across requests.  ### Retry - **Bounded attempts** — 3 total attempts (2 retries) per request. - **Retriable conditions** — HTTP 408 / 429 / 5xx and transient libcurl codes (connect / timeout class). Everything else — other 4xx, signature / not-found / auth failures — fails fast with no retry. - **Backoff** — exponential with jitter; the server's `Retry-After` header (429 / 503) is honored when present, capped at 30 s. - **Worst-case stuck time per request is bounded** — each attempt is capped by a curl   timeout (default 60 s); total ≈ timeout × attempts + backoff.  ## Non-S3 code changes  Most of the diff is the new `src/io/s3/` module and its tests. The changes to existing engine code are **additive and behavior-preserving for non-S3 paths**: `sirius_scan_manager` now holds a vector of backends and dispatches per path, and `SiriusContext` composes the S3 config from the YAML object-store settings — the local-file path is unchanged. The one behavior change touching a non-S3 path makes a scan_manager-routed local parquet scan fall back to cudf (instead of throwing) when no Sirius backend is configured, restoring the documented behavior under `dev`'s current default.  ## Testing  - **Local MinIO only, so far.** S3 integration tests run against a pinned MinIO container: `make s3-up` → `sirius_unittest \"[s3]\"` → `make s3-down` — 76 test cases, 0 failures. - An optional perf benchmark (`make s3-bench`) is included; it is hidden from the default est run and reachable only via that target.  ## TODO / follow-ups  - **Follow-up PR — SQL surface.** Make `gpu_execution('… read_parquet(\"s3://…\")')` route to the S3 backend end-to-end; - **AWS testing.** S3 integration is currently verified only against local MinIO. Once the   SQL surface lands and the path is end-to-end, add real-AWS testing.   ## Closes  Closes PR #759.  ---------  Co-authored-by: Yu <“ranyuan_jin@outlook.com“> Co-authored-by: Claude Opus 4.7 <noreply@anthropic.com>") | 2 months agoMay 21, 2026 |
| View all files |

## Repository files navigation

![Sirius](https://github.com/sirius-db/sirius/raw/dev/docs/logo/sirius_light_full.svg)

[![Slack](https://camo.githubusercontent.com/a235496dabebd1ec4bcea4edaead1676f36ca10077096d2674a1fe70342ab080/68747470733a2f2f696d672e736869656c64732e696f2f62616467652f536c61636b2d4a6f696e25323055732d626c75653f6c6f676f3d736c61636b)](https://join.slack.com/t/sirius-db/shared_invite/zt-33tuwt1sk-aa2dk0EU_dNjklSjIGW3vg)

Sirius is a GPU-native SQL engine. It plugs into existing databases such as DuckDB via the standard Substrait query format, requiring no query rewrites or major system changes. Sirius currently supports DuckDB and Doris (coming soon), other systems marked with \* are on our roadmap. Built on NVIDIA CUDA-X libraries including cuDF and RAPIDS Memory Manager (RMM), Sirius delivers high-performance GPU-accelerated analytics.

![Sirius architecture](https://github.com/sirius-db/sirius/raw/dev/docs/super-sirius-arch.png)

## Performance

[Permalink: Performance](https://github.com/sirius-db/sirius#performance)

Running TPC-H on 1TB data, Sirius accelerates DuckDB by 5x on DGX Station (GB300).

[![Performance](https://github.com/sirius-db/sirius/raw/dev/docs/super-sirius-perf.png)](https://github.com/sirius-db/sirius/blob/dev/docs/super-sirius-perf.png)

## Requirements

[Permalink: Requirements](https://github.com/sirius-db/sirius#requirements)

- Linux on amd64/x86\_64 or arm64/aarch64 with `glibc >= 2.28`.
- NVIDIA Turing or newer, with compute capability 7.5+.
- CUDA 13.x (driver 580.65.06 or newer) or CUDA 12.x (driver 525.60.13 or newer).
- `io_uring` enabled at runtime (`CONFIG_IO_URING`, `kernel.io_uring_disabled=0` recommended). Containers must allow `io_uring_setup`, `io_uring_enter`, and `io_uring_register`.
- Local Parquet files should be on a filesystem/block device that supports direct I/O (`O_DIRECT`).

### Build Requirements

[Permalink: Build Requirements](https://github.com/sirius-db/sirius#build-requirements)

- Git (to clone the repo)
- Pixi (install instructions [here](https://pixi.sh/latest/installation/))

## Building and Running Sirius

[Permalink: Building and Running Sirius](https://github.com/sirius-db/sirius#building-and-running-sirius)

For full build instructions, alternate build types, pre-commit setup, and testing, see [DEVELOPMENT.md](https://github.com/sirius-db/sirius/blob/dev/docs/DEVELOPMENT.md).

Quick start:

```
git clone --recurse-submodules https://github.com/sirius-db/sirius.git
cd sirius
pixi run make
./build/release/duckdb
```

Alternatively, load the extension into an existing DuckDB shell:

```
LOAD 'build/release/extension/sirius/sirius.duckdb_extension';
```

Either way, all DuckDB queries are automatically intercepted by the optimizer hook and run on GPU — no query rewrites required. Queries with unsupported operators fall back silently to CPU.

```
-- Plain SQL runs on GPU automatically
SELECT l_returnflag, sum(l_quantity)
FROM lineitem
GROUP BY l_returnflag
ORDER BY l_returnflag;

-- Disable transparent GPU execution for this connection
SET gpu_execution = false;
```

Two execution paths are available. See each page for build, configuration, and testing details:

- **[`gpu_execution`](https://github.com/sirius-db/sirius/blob/dev/docs/gpu_execution.md) (Recommended)** — Out-of-core execution with tiered memory management (GPU/host/disk), automatic data partitioning, and spilling. Works with **Parquet** data format.
- **[`gpu_processing`](https://github.com/sirius-db/sirius/blob/dev/docs/gpu_processing.md)** — In-memory execution where the dataset must fit in GPU memory. Works with DuckDB's native storage format.

## Configuration

[Permalink: Configuration](https://github.com/sirius-db/sirius#configuration)

Sirius loads its settings from a YAML config file, searched in this order:

1. Path in the `SIRIUS_CONFIG_FILE` environment variable
2. `./sirius.yaml` (current working directory)
3. `~/.sirius/sirius.yaml`

If no config file is found, built-in defaults apply (95% GPU memory, 8 GiB pinned host memory per NUMA node). See the [Configuration reference](https://github.com/sirius-db/sirius/blob/dev/docs/super-sirius/configuration.md) for all options: memory tiers, thread pools, operator parameters, and runtime `SET` variables. An example config is provided at [`test/cpp/integration/integration.yaml`](https://github.com/sirius-db/sirius/blob/dev/test/cpp/integration/integration.yaml).

## Logging

[Permalink: Logging](https://github.com/sirius-db/sirius#logging)

Sirius uses [spdlog](https://github.com/gabime/spdlog) for logging messages during query execution. Default log directory is `log` (relative to the current working directory) and default log level is `info`.

Log directory and level can be initialized via environment variables before loading the extension:

```
export SIRIUS_LOG_DIR=/path/to/logs
export SIRIUS_LOG_LEVEL=trace
```

Both can also be configured at runtime via DuckDB's `SET` command:

```
SET sirius_log_dir = '/path/to/logs';
SET sirius_log_level = 'trace';
SET sirius_log_flush_seconds = 1;
```

## Tracing

[Permalink: Tracing](https://github.com/sirius-db/sirius#tracing)

> **Note:** Tracing is experimental and the telemetry schema may change.

Sirius instruments query execution with [Quent](https://github.com/rapidsai/quent), emitting
per-query traces of operator and pipeline activity that Quent can render
as an interactive browser timeline. Enable it in your YAML config file,

```
sirius:
  telemetry:
    enable_quent: true
    output_directory: telemetry_data
```

run queries, then visualize:

```
pixi run quent   # serves Quent UI at http://localhost:8080
```

See the [Quent Telemetry guide](https://github.com/sirius-db/sirius/blob/dev/docs/super-sirius/quent-telemetry.md) for the full setup details: enabling the
exporter, per-query labeling, generating telemetry (using a TPC-H helper), and visualization.

## Limitations

[Permalink: Limitations](https://github.com/sirius-db/sirius#limitations)

Sirius is under active development. Notable current limitations include:

- **Data Type Coverage:** Sirius currently supports commonly used data types including `INTEGER`, `BIGINT`, `FLOAT`, `DOUBLE`, `VARCHAR`, `DATE`, `TIMESTAMP`, and `DECIMAL`. We are actively working on supporting additional data types—such as nested types.
- **Operator Coverage:** At present, Sirius supports `FILTER`, `PROJECTION`, `JOIN` (Hash/Nested Loop/Delim), `GROUP-BY`, `ORDER-BY`, `AGGREGATION`, `TOP-N`, `LIMIT`, and `CTE`. We are working on adding more advanced operators such as `WINDOW` functions and `ASOF JOIN`, etc.

For a full list of current limitations and ongoing work, please refer to our [GitHub issues page](https://github.com/sirius-db/sirius/issues). **If these issues are encountered when running Sirius, Sirius will gracefully fallback to DuckDB query execution on CPUs.**

## Contributors and Partners

[Permalink: Contributors and Partners](https://github.com/sirius-db/sirius#contributors-and-partners)

![Contributors and partners: NVIDIA, University of Wisconsin-Madison, DuckDB, and VAST Data](https://github.com/sirius-db/sirius/raw/dev/docs/logo/combined-logos-light.png)

## Future Roadmap

[Permalink: Future Roadmap](https://github.com/sirius-db/sirius#future-roadmap)

Sirius is still under major development and we are working on adding more features to Sirius, such as disk spilling, multi-GPUs, multi-node, more operators, data types, accelerating more engines, and many more.

Sirius always welcomes new contributors! If you are interested, check our [website](https://www.sirius-db.com/), reach out to our [email](https://github.com/sirius-db/sirius/blob/dev/docs/siriusdb@cs.wisc.edu), or join our [slack channel](https://join.slack.com/t/sirius-db/shared_invite/zt-33tuwt1sk-aa2dk0EU_dNjklSjIGW3vg).

**Let's kickstart the GPU eras for Data Analytics!**

## About

[www.sirius-db.com](https://www.sirius-db.com/ "https://www.sirius-db.com")

### Resources

[Readme](https://github.com/sirius-db/sirius#readme-ov-file)

### License

[Apache-2.0 license](https://github.com/sirius-db/sirius#Apache-2.0-1-ov-file)

### Contributing

[Contributing](https://github.com/sirius-db/sirius#contributing-ov-file)

### Uh oh!

There was an error while loading. [Please reload this page](https://github.com/sirius-db/sirius).

[Activity](https://github.com/sirius-db/sirius/activity)

[Custom properties](https://github.com/sirius-db/sirius/custom-properties)

### Stars

[**1k**\\
stars](https://github.com/sirius-db/sirius/stargazers)

### Watchers

[**14**\\
watching](https://github.com/sirius-db/sirius/watchers)

### Forks

[**104**\\
forks](https://github.com/sirius-db/sirius/forks)

[Report repository](https://github.com/contact/report-content?content_url=https%3A%2F%2Fgithub.com%2Fsirius-db%2Fsirius&report=sirius-db+%28user%29)

## [Releases](https://github.com/sirius-db/sirius/releases)

No releases published

## [Packages\  0](https://github.com/orgs/sirius-db/packages?repo_name=sirius)

No packages published

## [Contributors\  35](https://github.com/sirius-db/sirius/graphs/contributors)

- [![@bwyogatama](https://avatars.githubusercontent.com/u/34973829?s=64&v=4)](https://github.com/bwyogatama)
- [![@claude](https://avatars.githubusercontent.com/u/81847?s=64&v=4)](https://github.com/claude)
- [![@Yifei-yang7](https://avatars.githubusercontent.com/u/60085402?s=64&v=4)](https://github.com/Yifei-yang7)
- [![@mbrobbel](https://avatars.githubusercontent.com/u/5501570?s=64&v=4)](https://github.com/mbrobbel)
- [![@kevkrist](https://avatars.githubusercontent.com/u/56594638?s=64&v=4)](https://github.com/kevkrist)
- [![@felipeblazing](https://avatars.githubusercontent.com/u/16691757?s=64&v=4)](https://github.com/felipeblazing)
- [![@wmalpica](https://avatars.githubusercontent.com/u/16705032?s=64&v=4)](https://github.com/wmalpica)
- [![@aminaramoon](https://avatars.githubusercontent.com/u/13772400?s=64&v=4)](https://github.com/aminaramoon)
- [![@joosthooz](https://avatars.githubusercontent.com/u/1442581?s=64&v=4)](https://github.com/joosthooz)
- [![@sarda-devesh](https://avatars.githubusercontent.com/u/32046390?s=64&v=4)](https://github.com/sarda-devesh)
- [![@9prady9](https://avatars.githubusercontent.com/u/3270458?s=64&v=4)](https://github.com/9prady9)
- [![@mike-wendt](https://avatars.githubusercontent.com/u/1915404?s=64&v=4)](https://github.com/mike-wendt)
- [![@dependabot[bot]](https://avatars.githubusercontent.com/in/29110?s=64&v=4)](https://github.com/apps/dependabot)
- [![@ducndh](https://avatars.githubusercontent.com/u/63260696?s=64&v=4)](https://github.com/ducndh)

[\+ 21 contributors](https://github.com/sirius-db/sirius/graphs/contributors)

## Languages

- [C++79.2%](https://github.com/sirius-db/sirius/search?l=c%2B%2B)
- [Cuda8.0%](https://github.com/sirius-db/sirius/search?l=cuda)
- [Rust5.3%](https://github.com/sirius-db/sirius/search?l=rust)
- [Shell3.9%](https://github.com/sirius-db/sirius/search?l=shell)
- [Python2.4%](https://github.com/sirius-db/sirius/search?l=python)
- [CMake1.0%](https://github.com/sirius-db/sirius/search?l=cmake)
- [Makefile0.2%](https://github.com/sirius-db/sirius/search?l=makefile)

Generated from [duckdb/extension-template](https://github.com/duckdb/extension-template)

You can’t perform that action at this time.