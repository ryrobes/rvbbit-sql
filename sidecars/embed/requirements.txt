# torch + CUDA come from the base image. Just install the HTTP layer
# + tokenizer deps.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
transformers==4.46.3
# sentencepiece is required for BGE-M3's XLM-R tokenizer.
sentencepiece==0.2.0
